% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantum_foundation.R
\name{qf_krr_benchmark}
\alias{qf_krr_benchmark}
\title{Benchmark quantum-foundation KRR against classical baselines}
\usage{
qf_krr_benchmark(
  embeddings,
  covariates,
  y,
  train_ix = NULL,
  test_ix = NULL,
  n_pcs = 8L,
  reps = 2L,
  lambda = 0.1
)
}
\arguments{
\item{embeddings}{Foundation-model embedding matrix (n_obs x D).}

\item{covariates}{Raw-covariate matrix or data frame (n_obs x C).}

\item{y}{Numeric response.}

\item{train_ix, test_ix}{Integer index vectors selecting rows for
train/test.  If \code{NULL}, a 70/30 random split is drawn.}

\item{n_pcs, reps, lambda}{Passed to \code{\link[=qf_krr_fit]{qf_krr_fit()}}.}
}
\value{
A data frame with columns \code{method}, \code{rmse}, \code{mae}, \code{r2},
\code{n_train}, \code{n_test}.
}
\description{
Runs four regressors on the same train/test split:
\enumerate{
\item \code{ranger} (QRF)  over the RAW covariates (our established
plain-ML baseline from the v1.3 \code{case-cerrado-end-to-end}).
\item RBF Kernel Ridge Regression over the foundation-embedding PCs.
\item Quantum Kernel Ridge Regression over the RAW covariates
(Pillar 6 original).
\item Quantum Kernel Ridge Regression over the foundation-embedding
PCs (the v2.0.0 contribution).
}
}
\details{
Returns the test-set RMSE, MAE and R^2 for each setup so the
question "does the quantum lift on foundation embeddings beat
either its quantum-only or foundation-only ancestor?" gets an
empirical answer.
}
