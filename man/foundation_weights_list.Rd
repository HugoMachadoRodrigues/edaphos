% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/foundation_weights.R
\name{foundation_weights_list}
\alias{foundation_weights_list}
\title{Catalogue of pretrained Pillar 4 encoders}
\usage{
foundation_weights_list()
}
\value{
A data frame; one row per registered encoder.
}
\description{
Returns a data frame describing every pretrained encoder published
by the \code{edaphos} project: its name, the Zenodo DOI, the raster
AoI it was trained on, the number of input channels it expects,
the feature dimension of its embeddings, and the SHA-256 digest
of the hosted artefact.
}
\details{
New encoders are added by the \code{edaphos} maintainers on each minor
release and propagated through the package. Users with bespoke
pretrained encoders can bypass the registry entirely by passing a
local \code{.pt} path directly to \code{\link[=foundation_weights_load]{foundation_weights_load()}}.
}
\examples{
foundation_weights_list()
}
