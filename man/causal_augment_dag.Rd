% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_augment.R
\name{causal_augment_dag}
\alias{causal_augment_dag}
\title{Augment a base DAG with edges from a Knowledge Graph}
\usage{
causal_augment_dag(base_dag, kg, min_confidence = 0.7, allow_new_nodes = TRUE)
}
\arguments{
\item{base_dag}{A \code{dagitty} DAG (e.g. \code{\link[=causal_cerrado_dag]{causal_cerrado_dag()}}).}

\item{kg}{An \code{edaphos_causal_kg} (from \code{\link[=causal_kg_new]{causal_kg_new()}} / LLM
ingestion).}

\item{min_confidence}{Numeric in \verb{[0, 1]}; KG edges strictly
below this threshold are ignored.}

\item{allow_new_nodes}{Logical. When \code{TRUE} (default) nodes
appearing only in the KG are added to the augmented DAG. When
\code{FALSE} KG edges touching unseen nodes are dropped — useful
when the analyst wants to lock the structural vocabulary.}
}
\value{
An object of class \code{dagitty}, augmented.
}
\description{
Takes a \code{dagitty} DAG expressing prior / expert structural
knowledge (e.g. the CLORPT or Cerrado DAGs shipped with \code{edaphos})
and unions it with the subset of Knowledge-Graph edges whose
confidence is at least \code{min_confidence}. Duplicate edges are
dropped silently, and any edge whose insertion would introduce a
directed cycle is rejected (warned but not inserted), so the
returned DAG is guaranteed to be acyclic and usable for
\code{\link[=causal_adjustment_set]{causal_adjustment_set()}}.
}
