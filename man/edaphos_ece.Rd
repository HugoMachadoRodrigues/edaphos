% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edaphos_metrics.R
\name{edaphos_ece}
\alias{edaphos_ece}
\title{Expected calibration error (ECE) for a regression reliability diagram}
\usage{
edaphos_ece(observed, predicted_quantiles, quantile_levels)
}
\arguments{
\item{observed}{Numeric vector.}

\item{predicted_quantiles}{A numeric matrix with one column per
nominal quantile level in \code{quantile_levels}.}

\item{quantile_levels}{Numeric vector in \verb{(0, 1)} giving the
nominal level of each column of \code{predicted_quantiles}.}
}
\value{
Numeric scalar (mean of per-level absolute calibration
errors).
}
\description{
Bins predictions by their predicted quantile, then within each bin
compares the empirical miscoverage rate to the nominal level.
Lower is better; 0 = perfect calibration.
}
\details{
For each quantile level \eqn{q_k = k / K} we compute the fraction
of test points whose observed value is below the predicted
\eqn{q_k} quantile and compare to the nominal level.
}
