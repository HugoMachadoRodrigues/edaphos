% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantum_foundation.R
\name{qf_kernel_compare}
\alias{qf_kernel_compare}
\title{Compare quantum, RBF, and linear kernels on the same feature set}
\usage{
qf_kernel_compare(X_q, reps = 2L, rbf_sigma = NULL)
}
\arguments{
\item{X_q}{PCA-reduced, pi-scaled feature matrix (the \code{X_q}
element of \code{\link[=qf_embed_reduce]{qf_embed_reduce()}}).}

\item{reps}{Integer; ZZFeatureMap repetitions.  Default \code{2L}.}

\item{rbf_sigma}{Numeric; RBF kernel bandwidth.  If \code{NULL},
uses the median heuristic
\verb{median( || x_i - x_j || )} over the training set.}
}
\value{
Named list with \code{K_quantum}, \code{K_rbf}, \code{K_linear}, and a
\code{diagnostics} data frame summarising pairwise distances +
effective rank.
}
\description{
Computes three Gram matrices on the same PCA-reduced embeddings
and reports their pairwise Frobenius distance and eigenvalue
divergence.  Useful for understanding whether the quantum kernel
is materially different from the classical RBF at the same
feature space (if not, the quantum lift is cosmetic).
}
