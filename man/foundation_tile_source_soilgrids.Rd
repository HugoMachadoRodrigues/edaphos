% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/foundation_tiles.R
\name{foundation_tile_source_soilgrids}
\alias{foundation_tile_source_soilgrids}
\title{Fetch a SoilGrids 250 m stack over an AoI}
\usage{
foundation_tile_source_soilgrids(
  variables = c("soc", "clay"),
  depth = "0-5cm",
  stat = "mean",
  aoi,
  path = tempdir()
)
}
\arguments{
\item{variables}{Character vector of SoilGrids variable codes.
Common choices: \code{"soc"} (organic carbon), \code{"clay"}, \code{"sand"},
\code{"phh2o"} (pH in water), \code{"cec"}, \code{"bdod"} (bulk density),
\code{"nitrogen"}, \code{"cfvo"} (coarse fragments).}

\item{depth}{Character, one of \code{"0-5cm"}, \code{"5-15cm"}, \code{"15-30cm"},
\code{"30-60cm"}, \code{"60-100cm"}, \code{"100-200cm"}.}

\item{stat}{Character, one of \code{"mean"}, \code{"Q0.05"}, \code{"Q0.5"},
\code{"Q0.95"}.}

\item{aoi}{A \code{terra::SpatExtent}, \code{sf} bounding box, or a length-4
numeric vector \code{c(xmin, xmax, ymin, ymax)} in WGS84 degrees.}

\item{path}{Directory where \code{geodata} caches raw downloads
(defaults to \code{tempdir()}).}
}
\value{
A \code{terra::SpatRaster} with one layer per requested variable,
cropped to the AoI.
}
\description{
Thin convenience wrapper around \code{geodata::soil_world()} that pulls
the listed variables, crops them to the area of interest, and
returns them as a single \code{terra::SpatRaster} with one layer per
variable. SoilGrids is \strong{public and keyless} so no authentication
is required.
}
