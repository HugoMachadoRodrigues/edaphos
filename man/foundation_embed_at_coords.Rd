% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/foundation_embed_coords.R
\name{foundation_embed_at_coords}
\alias{foundation_embed_at_coords}
\title{Extract foundation-model embeddings at a set of query coordinates}
\usage{
foundation_embed_at_coords(
  moco,
  coords,
  stack,
  dataset,
  patch_size = NULL,
  projection = FALSE,
  batch_size = 32L
)
}
\arguments{
\item{moco}{An \code{edaphos_foundation_moco} (as returned by
\code{\link[=foundation_weights_load]{foundation_weights_load()}} or \code{\link[=foundation_moco_pretrain]{foundation_moco_pretrain()}}).}

\item{coords}{A data frame or matrix with two columns \code{"lon"} and
\code{"lat"} in the same CRS as \code{stack}.  Order of rows is preserved.}

\item{stack}{A \code{terra::SpatRaster} with one layer per channel.  The
number of layers must equal \code{dataset$n_channels}.}

\item{dataset}{An \code{edaphos_tile_dataset} (or any list carrying
\code{patch_size}, \code{n_channels}, \code{means}, \code{sds} fields).}

\item{patch_size}{Integer; side length of the square patch.
Defaults to \code{dataset$patch_size}.}

\item{projection}{Logical; if \code{TRUE} return the L2-normalised
projection-head outputs instead of the backbone features.}

\item{batch_size}{Integer; number of patches to forward through the
encoder in a single call (trades memory for speed).  Defaults
to \code{32L}.}
}
\value{
A numeric matrix with \code{nrow(coords)} rows and \code{D} columns,
where \code{D} is \code{moco$feature_dim} or \code{moco$proj_dim}.  Rows that
could not be extracted contain \code{NA}.
}
\description{
For each row of \code{coords} (longitude, latitude) crops a
\verb{patch_size x patch_size} window out of the raster \code{stack},
normalises every channel with the training means / sds stored in
\code{dataset}, and runs the MoCo v2 encoder forward to get one
embedding vector per query point.  Returns the resulting
\verb{(n_coords, D)} matrix where \code{D} is either the backbone feature
dimension (when \code{projection = FALSE}, the default) or the
projection-head output dimension.
}
\details{
Compared with \code{\link[=foundation_moco_embed_raster]{foundation_moco_embed_raster()}} (which runs the
encoder over a regular stride-based grid), this function:
\itemize{
\item reads only \code{n_coords} patches, not the full grid, so extraction
at 1 095 WoSIS profiles is O(minutes) rather than O(hour) even on
a 2 deg x 2 deg Cerrado cube;
\item returns a tidy \code{matrix} (rows aligned with \code{coords}) ready to
hand directly to \code{\link[=causal_iv_from_embeddings]{causal_iv_from_embeddings()}} as the
instrument matrix.
}

Coordinates that fall outside the raster extent, or whose patch
would cross the raster edge, are returned as a row of \code{NA}.
}
