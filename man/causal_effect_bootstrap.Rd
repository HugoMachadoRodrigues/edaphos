% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_posterior.R
\name{causal_effect_bootstrap}
\alias{causal_effect_bootstrap}
\title{Block-bootstrap the backdoor-adjusted direct effect}
\usage{
causal_effect_bootstrap(
  data,
  dag,
  exposure,
  outcome,
  adjustment = NULL,
  cluster = "kmeans_cluster",
  B = 500L,
  effect = c("direct", "total"),
  seed = NULL
)
}
\arguments{
\item{data}{A data frame with the exposure, outcome, adjustment
columns and a cluster-id column.}

\item{dag}{A \code{dagitty} DAG (only used when \code{adjustment} is \code{NULL}).}

\item{exposure, outcome}{Character; column names.}

\item{adjustment}{Character vector of adjustment-set column names
(defaults to the minimal set from the DAG).}

\item{cluster}{Character; name of the cluster-id column in \code{data}.}

\item{B}{Integer; number of bootstrap resamples. Defaults to \code{500L}.}

\item{effect}{One of \code{"direct"}, \code{"total"} (passed to the adjustment
set resolver when \code{adjustment = NULL}).}

\item{seed}{Optional integer for reproducibility.}
}
\value{
Numeric vector of length \code{B} with one direct-effect estimate
per bootstrap resample.
}
\description{
Resamples clusters (not rows) with replacement and refits the
adjusted OLS on each resample, returning the vector of direct-effect
coefficients. When the clustering structure is k-means on \verb{(lon, lat)} (as in the Cerrado pipeline from v1.4.0 onward), this produces
spatial-clustering-aware confidence intervals that the asymptotic
\code{confint(lm)} interval ignores.
}
