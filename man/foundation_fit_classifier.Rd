% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/foundation_finetune.R
\name{foundation_fit_classifier}
\alias{foundation_fit_classifier}
\title{Fine-tune or linearly probe a Pillar 4 encoder for classification}
\usage{
foundation_fit_classifier(
  encoder,
  x,
  y,
  freeze_backbone = TRUE,
  head = c("linear", "mlp"),
  hidden = c(64L, 32L),
  dropout = 0,
  epochs = 30L,
  batch_size = 32L,
  lr = 0.001,
  weight_decay = 0,
  backbone_lr_mult = 0.1,
  val_split = 0.2,
  device = c("cpu", "mps", "cuda"),
  seed = NULL,
  verbose = FALSE
)
}
\arguments{
\item{encoder}{An \code{edaphos_foundation_moco} or
\code{edaphos_foundation_simclr} fit, or a bare \code{nn_module} that
accepts \verb{(batch, C, H, W)} input and returns either an embedding
tensor or a list with a \code{feature} field.}

\item{x}{A 4-dimensional R array \verb{(N, C, H, W)} of labelled patches,
of the same shape that was used to pretrain the encoder.}

\item{y}{A factor (or coercible character / integer) of length \code{N}
with the patch-level class labels.}

\item{freeze_backbone}{Logical — \code{TRUE} for linear probing (default),
\code{FALSE} for full fine-tuning. When \code{FALSE}, the encoder is put
into training mode and its gradients flow back through the
classification loss.}

\item{head}{\code{"linear"} (default) or \code{"mlp"}.}

\item{hidden}{Integer vector with MLP hidden-layer widths when
\code{head = "mlp"}. Default \code{c(64L, 32L)}.}

\item{dropout}{Dropout probability in the MLP head (0 = disabled).}

\item{epochs, batch_size, lr}{Training hyperparameters.}

\item{weight_decay}{Adam weight decay (L2 regularisation).}

\item{backbone_lr_mult}{Multiplicative factor applied to \code{lr} for
the encoder parameters when \code{freeze_backbone = FALSE}. Default
\code{0.1}; set to \code{1.0} to train the backbone at full speed.}

\item{val_split}{Fraction of \code{x} held out for validation. \code{0}
disables validation.}

\item{device}{\code{"cpu"} (default), \code{"mps"} or \code{"cuda"}. When the
requested backend is unavailable the function falls back to
\code{"cpu"} with a message.}

\item{seed}{Optional integer — seeds torch, NumPy and the
train/val split.}

\item{verbose}{Logical — print loss / accuracy every 10 epochs.}
}
\value{
An \code{edaphos_foundation_classifier} list with:
\describe{
\item{encoder, head}{The trained torch modules.}
\item{classes}{The character factor levels used for prediction.}
\item{loss_history, val_accuracy_history}{Per-epoch training loss
and validation accuracy (NA when \code{val_split = 0}).}
\item{config}{List of the inputs to
\code{\link[=foundation_fit_classifier]{foundation_fit_classifier()}} for reproducibility.}
}
}
\description{
Attaches a classification head on top of a self-supervised encoder
produced by \code{\link[=foundation_moco_pretrain_tiles]{foundation_moco_pretrain_tiles()}},
\code{\link[=foundation_moco_pretrain]{foundation_moco_pretrain()}} or \code{\link[=foundation_simclr_pretrain]{foundation_simclr_pretrain()}} and
trains it against a labelled patch set. Two standard regimes from
the transfer-learning literature are supported:
}
\details{
\describe{
\item{Linear probe (\code{freeze_backbone = TRUE}, \code{head = "linear"})}{
The encoder weights are frozen; only a single
\code{nn_linear(feature_dim, n_classes)} head is trained. This is the
canonical benchmark for evaluating self-supervised
representations as fixed feature extractors
(He, Girshick and Dollar 2019).}
\item{Full fine-tuning (\code{freeze_backbone = FALSE})}{The encoder
and the head are trained jointly, with a two-group learning
rate (\code{lr * backbone_lr_mult} for the backbone, \code{lr} for the
head). Usually the better option when the downstream dataset is
large enough (≥ ~500 patches per class).}
}
}
\examples{
\dontrun{
  ds <- readRDS("tools/pretrain/cerrado_dataset.rds")
  moco <- foundation_weights_load("edaphos-cerrado-moco-v1")

  # Label a subset of patches by soil order (synthetic example).
  patches <- array(rnorm(200 * ds$n_channels * 16 * 16),
                   dim = c(200, ds$n_channels, 16, 16))
  soil_order <- factor(sample(c("Oxisol", "Ultisol", "Inceptisol"),
                               200, replace = TRUE))

  fit <- foundation_fit_classifier(
    moco, patches, soil_order,
    freeze_backbone = TRUE, head = "linear",
    epochs = 40L, device = "mps", seed = 1L
  )
  pred <- predict(fit, patches, type = "class")
}
}
\references{
He, K., Girshick, R. and Dollar, P. (2019). Rethinking ImageNet
pre-training. \emph{ICCV 2019}.

Kornblith, S., Shlens, J. and Le, Q. V. (2019). Do better ImageNet
models transfer better? \emph{CVPR 2019}.
}
\seealso{
\code{\link[=predict.edaphos_foundation_classifier]{predict.edaphos_foundation_classifier()}},
\code{\link[=foundation_fit_regressor]{foundation_fit_regressor()}}, \code{\link[=foundation_moco_pretrain_tiles]{foundation_moco_pretrain_tiles()}}.
}
