% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/foundation_moco.R
\name{foundation_moco_embed}
\alias{foundation_moco_embed}
\title{Extract backbone embeddings from a fitted MoCo v2 encoder}
\usage{
foundation_moco_embed(object, patches, projection = FALSE)
}
\arguments{
\item{object}{A \code{edaphos_foundation_moco}.}

\item{patches}{Array \verb{(N, C, H, W)}.}

\item{projection}{Logical; return L2-normalised projection outputs
rather than backbone features.}
}
\value{
Numeric matrix \verb{(N, D)} where \code{D = feature_dim} (or
\code{proj_dim} if \code{projection = TRUE}).
}
\description{
Returns the \code{feature_dim} backbone output of the \strong{query} encoder
(the projection head is by design discarded after pre-training, so
the returned embedding is the reusable representation suitable for
downstream DSM tasks). Set \code{projection = TRUE} to obtain the
normalised projection-head output instead, which is useful when
visualising the contrastive space.
}
