% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/foundation_posterior.R
\name{foundation_finetune_ensemble}
\alias{foundation_finetune_ensemble}
\title{Deep-ensemble fine-tune of a Pillar 4 foundation encoder}
\usage{
foundation_finetune_ensemble(
  encoder,
  x,
  y,
  task = c("classification", "regression"),
  K_ens = 5L,
  base_seed = 301L,
  ...
)
}
\arguments{
\item{encoder}{A MoCo/SimCLR encoder as returned by
\code{\link[=foundation_weights_load]{foundation_weights_load()}} (or an equivalent \code{nn_module}).}

\item{x, y}{Training data; same shape requirements as the base
fine-tune functions (4-D array \verb{(N, C, H, W)} + vector \code{y}).}

\item{task}{\code{"classification"} or \code{"regression"}.}

\item{K_ens}{Integer; number of ensemble heads. Defaults to \code{5L}.}

\item{base_seed}{Integer; each member uses \code{base_seed + k - 1L}.}

\item{...}{Additional arguments forwarded verbatim to
\code{\link[=foundation_fit_classifier]{foundation_fit_classifier()}} or \code{\link[=foundation_fit_regressor]{foundation_fit_regressor()}}
(e.g. \code{epochs}, \code{batch_size}, \code{lr}, \code{dropout}, \code{hidden},
\code{freeze_backbone}, \code{device}, \code{verbose}).}
}
\value{
A list with class \code{edaphos_foundation_ensemble} containing
\code{members} (list of K fits), \code{task}, \code{K_ens}, \code{encoder} (a
reference to the fit-time encoder), and \code{final_losses} /
\code{loss_histories}.
}
\description{
Trains \code{K_ens} independent heads on the same encoder with
different random seeds and collects them into a single object.
Each member is a full \code{\link[=foundation_fit_classifier]{foundation_fit_classifier()}} or
\code{\link[=foundation_fit_regressor]{foundation_fit_regressor()}} fit.
}
\references{
Lakshminarayanan, B., Pritzel, A. and Blundell, C. (2017). Simple
and scalable predictive uncertainty estimation using deep
ensembles. \emph{NeurIPS 30}.
}
