% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/foundation_moco.R
\name{foundation_moco_pretrain_tiles}
\alias{foundation_moco_pretrain_tiles}
\title{Dataset-backed MoCo v2 pre-training for planetary-scale corpora}
\usage{
foundation_moco_pretrain_tiles(
  dataset,
  feature_dim = 64L,
  proj_dim = 32L,
  queue_size = 1024L,
  momentum = 0.999,
  temperature = 0.07,
  batch_size = 16L,
  epochs = 100L,
  lr = 0.03,
  crop_ratio = c(0.6, 1),
  flip_prob = 0.5,
  rot90_prob = 0.75,
  channel_drop_prob = 0.2,
  cutout_prob = 0.3,
  cutout_size_ratio = 0.2,
  brightness_jitter = 0.2,
  noise_sd = 0.1,
  seed = NULL,
  verbose = FALSE,
  checkpoint_dir = NULL,
  checkpoint_every = 10L,
  resume = NULL
)
}
\arguments{
\item{dataset}{An \code{edaphos_tile_dataset}.}

\item{feature_dim, proj_dim, queue_size, momentum, temperature, batch_size, epochs, lr, crop_ratio, flip_prob, rot90_prob, channel_drop_prob, cutout_prob, cutout_size_ratio, brightness_jitter, noise_sd, seed, verbose}{As in \code{\link[=foundation_moco_pretrain]{foundation_moco_pretrain()}}.}

\item{checkpoint_dir}{Optional directory for periodic checkpoints.}

\item{checkpoint_every}{Integer -- save a checkpoint every \code{k}
epochs.}

\item{resume}{Optional path to a checkpoint directory to restart
from.}
}
\value{
An \code{edaphos_foundation_moco} (identical structure to
\code{\link[=foundation_moco_pretrain]{foundation_moco_pretrain()}}).
}
\description{
Drop-in streaming variant of \code{\link[=foundation_moco_pretrain]{foundation_moco_pretrain()}} that
reads patches on the fly from an \code{edaphos_tile_dataset}
(see \code{\link[=foundation_tile_dataset]{foundation_tile_dataset()}}). The point is to train on real
multi-source raster mosaics -- SoilGrids, WorldClim, SRTM, MODIS,
ERA5 -- that do not fit in RAM as a single \verb{(N, C, H, W)} array.
}
\details{
Checkpointing is optional but recommended for long runs: every
\code{checkpoint_every} epochs the encoder state dicts, the dictionary
queue, the loss history and the configuration are written to
\code{checkpoint_dir}. Pass \code{resume = path} to restart from that
checkpoint.
}
