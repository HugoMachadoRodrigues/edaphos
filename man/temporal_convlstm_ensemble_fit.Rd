% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal_posterior.R
\name{temporal_convlstm_ensemble_fit}
\alias{temporal_convlstm_ensemble_fit}
\title{K-seed deep ensemble of stacked ConvLSTMs}
\usage{
temporal_convlstm_ensemble_fit(
  sequence,
  target,
  hidden_dims = c(8L, 4L),
  kernel_size = 3L,
  return_sequence = FALSE,
  epochs = 80L,
  lr = 0.02,
  K_ens = 10L,
  base_seed = 101L,
  physics_lambda = 0,
  physics_k_in = 0.03,
  physics_k_out = 0.015,
  physics_driver_channel = 2L,
  verbose = FALSE
)
}
\arguments{
\item{sequence, target, hidden_dims, kernel_size, return_sequence, epochs, lr, physics_lambda, physics_k_in, physics_k_out, physics_driver_channel, verbose}{Passed through unchanged to \code{\link[=temporal_convlstm_fit]{temporal_convlstm_fit()}}.}

\item{K_ens}{Integer; number of ensemble members. Defaults to
\code{10L}.}

\item{base_seed}{Integer; each member uses \code{base_seed + k - 1L}.}
}
\value{
A list with class \code{edaphos_temporal_convlstm_ensemble}
containing
\describe{
\item{members}{List of K \code{edaphos_temporal_convlstm} fits.}
\item{K_ens}{Integer, the ensemble size.}
\item{final_losses}{Numeric vector of per-member final training
losses.}
\item{loss_histories}{List of K numeric vectors (one per member).}
}
}
\description{
Trains \code{K_ens} independent \code{\link[=temporal_convlstm_fit]{temporal_convlstm_fit()}} models with
different random seeds and collects them in a single object. This
formalises the hand-rolled ensemble loop from the v1.5.0 Pillar 3
Cerrado runner (\code{data-raw/temporal_cerrado_run.R}) and produces the
natural forecast-ensemble input for \code{\link[=temporal_kalman_update]{temporal_kalman_update()}} or
for \code{\link[=as_edaphos_posterior]{as_edaphos_posterior()}}.
}
\seealso{
\code{\link[=temporal_convlstm_rollout]{temporal_convlstm_rollout()}} to roll each member forward,
\code{\link[=temporal_kalman_update]{temporal_kalman_update()}} to assimilate observations into the
forecast ensemble, \code{\link[=as_edaphos_posterior]{as_edaphos_posterior()}} for the unified
uncertainty wrapper.
}
