% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantum_posterior.R
\name{quantum_krr_posterior}
\alias{quantum_krr_posterior}
\title{GP-equivalent posterior for a Quantum Kernel Ridge Regression fit}
\usage{
quantum_krr_posterior(object, newdata, n_samples = 500L, units = NULL)
}
\arguments{
\item{object}{An \code{edaphos_quantum_krr}.}

\item{newdata}{A matrix (or data frame) with \code{ncol(newdata) == object$n_qubits}.}

\item{n_samples}{Integer; the Gaussian posterior is analytic, so
sampling is only needed for the \code{edaphos_posterior} machinery
(CRPS estimation etc.). Defaults to \code{500L}.}

\item{units}{Optional free-text units tag.}
}
\value{
An \code{edaphos_posterior} with \code{method = "analytic"} and
\code{query_type = "sample"}. The epistemic/aleatoric decomposition
is carried through \code{post$epistemic_sd} and \code{post$aleatoric_sd}.
}
\description{
Given a fitted \code{\link[=quantum_krr_fit]{quantum_krr_fit()}} model, returns the predictive
posterior at new inputs as an \code{\link[=edaphos_posterior]{edaphos_posterior()}}. Using the
well-known equivalence between Kernel Ridge Regression and
Gaussian-process regression (Rasmussen & Williams 2006, §2.3), the
predictive variance is derived analytically from the same gram
matrix \verb{K + lambda I} that produces the point prediction. Aleatoric
noise is estimated from leave-one-out residuals.
}
\references{
Rasmussen, C. E. and Williams, C. K. I. (2006). \emph{Gaussian Processes
for Machine Learning}. MIT Press, §2.3.
}
