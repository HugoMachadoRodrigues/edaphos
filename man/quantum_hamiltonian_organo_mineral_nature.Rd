% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantum_nature.R
\name{quantum_hamiltonian_organo_mineral_nature}
\alias{quantum_hamiltonian_organo_mineral_nature}
\title{Organo-mineral Hamiltonians derived from ab initio molecular models}
\usage{
quantum_hamiltonian_organo_mineral_nature(
  variant = c("formic_acid", "methanediol", "ferric_formate"),
  basis = "sto3g",
  num_active_electrons = NULL,
  num_active_orbitals = NULL,
  mapper = c("parity", "jordan_wigner", "bravyi_kitaev")
)
}
\arguments{
\item{variant}{Character — \code{"formic_acid"} (default),
\code{"methanediol"} or \code{"ferric_formate"}.}

\item{basis}{Character — PySCF basis set label. Default
\code{"sto3g"} for speed; upgrade to \code{"631g"} for quantitative work.}

\item{num_active_electrons, num_active_orbitals}{Optional integer
overrides for the active-space size. When left \code{NULL} the
variant-specific default is used (\verb{(2, 2)} for the closed-shell
organics, \verb{(4, 4)} for ferric formate).}

\item{mapper}{Character — fermion-to-qubit mapping; see
\code{\link[=quantum_hamiltonian_from_pyscf]{quantum_hamiltonian_from_pyscf()}}.}
}
\value{
An \code{edaphos_quantum_hamiltonian_nature} (which inherits
from \code{edaphos_quantum_hamiltonian}) carrying the qubit
Hamiltonian plus the nuclear-repulsion, frozen-core and
reference-energy attributes needed to reconstruct the total
molecular energy.
}
\description{
Curated presets that build an \code{edaphos_quantum_hamiltonian} from a
realistic molecular geometry through
\code{\link[=quantum_hamiltonian_from_pyscf]{quantum_hamiltonian_from_pyscf()}}. Three variants are shipped,
each chosen to expose a distinct piece of organo-mineral chemistry
at a qubit count that is accessible to present-day hardware:
}
\details{
\describe{
\item{\code{"formic_acid"} (default)}{HCOOH in STO-3G with the chemical
core frozen and a \verb{(2e, 2o)} active space. Models the
\strong{carboxylate functional group} that is the dominant
coordinating moiety of humic acids. After parity tapering the
qubit Hamiltonian is 2 qubits / ~5 Pauli terms — fast enough
to run on any hardware and small enough for CI.}
\item{\code{"methanediol"}}{H\eqn{_2}C(OH)\eqn{_2} in STO-3G with the
core frozen and a \verb{(2e, 2o)} active space. Models the
\strong{ortho-diol} motif that underpins catechol-type Fe(III)
chelation in humic substances. 2 qubits after tapering.}
\item{\code{"ferric_formate"}}{Monodentate Fe(III)–OOCH complex in
STO-3G. Open-shell (S = 5/2), requires a \verb{(4e, 4o)} active
space around Fe 3d + carboxylate \eqn{\pi^*}, and produces a
4-qubit Hamiltonian after parity tapering. This variant is
the \emph{minimum viable} representation of an organo-mineral
coordination event at the clay–humus interface; because the
SCF convergence is sensitive to the initial guess we
recommend running it on \code{backend = "aer_shots"} or
\code{"ibmq"} with \code{"SPSA"} and at least 100 iterations, and
consulting \code{attr(ham, "reference_energy")} as a sanity
check.}
}
}
\examples{
\dontrun{
  ham <- quantum_hamiltonian_organo_mineral_nature("formic_acid")
  attr(ham, "nuclear_repulsion_energy")
  quantum_vqe_fit(ham, seed = 1L)$energy +
    attr(ham, "nuclear_repulsion_energy")
}
}
\references{
Stevenson, F. J. (1994). \emph{Humus Chemistry: Genesis, Composition,
Reactions}. Wiley. (Chapter on carboxylate and catechol-type
chelation of Fe(III) at mineral surfaces.)
}
\seealso{
\code{\link[=quantum_hamiltonian_from_pyscf]{quantum_hamiltonian_from_pyscf()}},
\code{\link[=quantum_hamiltonian_organo_mineral]{quantum_hamiltonian_organo_mineral()}} (toy 4-qubit variant),
\code{\link[=quantum_vqe_fit]{quantum_vqe_fit()}}.
}
