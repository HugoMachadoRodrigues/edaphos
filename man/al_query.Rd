% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/active_query.R
\name{al_query}
\alias{al_query}
\title{Query the most informative unlabeled candidates}
\usage{
al_query(
  model,
  candidates,
  n = 5L,
  strategy = c("hybrid", "uncertainty", "diverse", "cost"),
  alpha = 0.7,
  quantiles = c(0.1, 0.9),
  base = NULL,
  cost_weight = 0.3,
  physics_gate = NULL
)
}
\arguments{
\item{model}{A \code{edaphos_al_model}.}

\item{candidates}{Data frame of unlabeled candidate locations. Must
contain \code{model$covariates}. Rows with \code{NA} in any covariate are
ignored.}

\item{n}{Integer, batch size.}

\item{strategy}{One of \code{"hybrid"}, \code{"uncertainty"}, \code{"diverse"},
\code{"cost"}.}

\item{alpha}{Numeric in \verb{[0, 1]}, weight of uncertainty vs diversity in
\code{hybrid} / \code{cost}.}

\item{quantiles}{Length-2 numeric with the lower/upper probabilities
used to measure the QRF interval width. Defaults to \code{c(0.1, 0.9)}.}

\item{base}{Numeric length-2 vector \code{c(x, y)}; required by
\code{strategy = "cost"}.}

\item{cost_weight}{Numeric, weight of the cost penalty in \code{"cost"}.}

\item{physics_gate}{Optional function
\verb{function(candidates, predicted_mean) -> logical} that returns
\code{TRUE} for physically feasible candidates and \code{FALSE} for
infeasible ones. Infeasible rows are excluded from the greedy
selection, linking Pillar 5 to Pillar 2 — see
\code{\link[=al_physics_gate_piml]{al_physics_gate_piml()}} for a ready-made gate driven by a PIML
profile fit.}
}
\value{
Integer vector of row indices in \code{candidates} that form the
next batch, in selection order.
}
\description{
Given a fitted Active-Learning model, picks the next \code{n} candidates to
sample according to a trade-off strategy. All strategies use a \strong{greedy
batch-mode} selection: the second and later picks within the same call
see the previously picked points as if they were already labeled, so the
batch remains internally diverse.
}
\section{Strategies}{
\itemize{
\item \code{"uncertainty"} — highest QRF prediction-interval width (pure
exploitation of model uncertainty).
\item \code{"diverse"} — max-min distance in the \emph{standardised} covariate space
from both the current labeled set and the already-picked batch
members (pure exploration).
\item \code{"hybrid"} — convex combination
\code{alpha * uncertainty + (1 - alpha) * diversity} on 0-1 scaled scores.
The recommended default.
\item \code{"cost"} — \code{"hybrid"} minus \code{cost_weight * cost}, where \code{cost} is the
0-1 normalised Euclidean distance to a logistical \code{base} (x, y). Use
this to steer an autonomous sampler (drone, rover) towards points it
can physically reach with limited energy.
}
}

