% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uncertainty.R
\name{uncertainty_calibrate}
\alias{uncertainty_calibrate}
\title{Calibration diagnostics for an \code{edaphos_posterior}}
\usage{
uncertainty_calibrate(post, truth, probs = seq(0.05, 0.95, by = 0.05))
}
\arguments{
\item{post}{An \code{edaphos_posterior}.}

\item{truth}{Numeric array with the same shape as \code{post$mean};
the ground-truth values.}

\item{probs}{Nominal coverage probabilities at which to report
PICP + MPIW, and for the reliability curve. Defaults to
\code{seq(0.05, 0.95, by = 0.05)}.}
}
\value{
A list with:
\describe{
\item{crps}{Mean sample-based CRPS across query cells.}
\item{picp}{Named vector of empirical coverage, one per \code{probs}.}
\item{mpiw}{Named vector of mean interval widths, one per \code{probs}.}
\item{reliability_df}{Data frame with columns \code{nominal},
\code{empirical}, \code{diff} suitable for \code{ggplot2::geom_line}.}
\item{point_rmse}{Root-mean-squared error of the posterior mean
against the truth.}
}
}
\description{
Computes the continuous ranked probability score (CRPS), a
prediction-interval coverage probability (PICP) at each requested
nominal level, the mean prediction-interval width (MPIW) at
the same levels, and a ready-for-ggplot reliability data frame.
}
\details{
The CRPS for a sample-based posterior \code{F} and a scalar truth \code{y}
is computed from the Monte-Carlo formula
\eqn{
  \mathrm{CRPS}(F, y) = \tfrac{1}{N}\sum_i |s_i - y|
                         - \tfrac{1}{2N^2}\sum_{i,j} |s_i - s_j|
}
(see Gneiting & Raftery 2007 for the derivation and its strictly
proper scoring rule interpretation). The per-query CRPS is
averaged across all query cells for a single reported scalar.
}
\references{
Gneiting, T. and Raftery, A. E. (2007). Strictly proper scoring
rules, prediction, and estimation. \emph{Journal of the American
Statistical Association} \strong{102}(477), 359-378.
}
