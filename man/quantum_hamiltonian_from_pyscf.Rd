% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantum_nature.R
\name{quantum_hamiltonian_from_pyscf}
\alias{quantum_hamiltonian_from_pyscf}
\title{Build a quantum Hamiltonian from a molecular geometry}
\usage{
quantum_hamiltonian_from_pyscf(
  atom,
  basis = "sto3g",
  charge = 0L,
  spin = 0L,
  freeze_core = TRUE,
  num_active_electrons = NULL,
  num_active_orbitals = NULL,
  mapper = c("parity", "jordan_wigner", "bravyi_kitaev")
)
}
\arguments{
\item{atom}{Character — the PySCF atom specification, one atom
per semicolon-separated block, e.g.
\code{"H 0 0 0; H 0 0 0.735"} (H2 at 0.735 Angstroms) or
\code{"C 0 0 0; O 1.21 0 0; O -0.63 1.08 0; H -0.3 -0.99 0; H -1.56 0.85 0"}
(formic acid).}

\item{basis}{Character — the atomic-orbital basis set. Default
\code{"sto3g"}. Use \code{"631g"}, \code{"ccpvdz"}, or larger for chemistry-
grade accuracy at the cost of more qubits.}

\item{charge}{Integer — molecular charge. Default \code{0}.}

\item{spin}{Integer — \verb{2S}, i.e. number of unpaired electrons.
Default \code{0} (closed shell).}

\item{freeze_core}{Logical — apply the frozen-core approximation
before the active-space transformation. Default \code{TRUE}.}

\item{num_active_electrons}{Optional integer — size of the active
electron space (after freezing the core). When \code{NULL}, no
active-space reduction is applied (all frontier orbitals kept).}

\item{num_active_orbitals}{Optional integer — size of the active
orbital space.}

\item{mapper}{Character — fermion-to-qubit mapping. One of
\code{"parity"} (default, with tapering by particle-number symmetry),
\code{"jordan_wigner"}, \code{"bravyi_kitaev"}.}
}
\value{
An \code{edaphos_quantum_hamiltonian} carrying the Pauli-string
Hamiltonian of the active-space problem. The object additionally
exposes the following attributes:
\describe{
\item{\code{nuclear_repulsion_energy}}{Nuclear-nuclear Coulomb
repulsion (constant).}
\item{\code{frozen_core_shift}}{Constant energy shift introduced by
the frozen-core transformation (0 when
\code{freeze_core = FALSE}).}
\item{\code{reference_energy}}{Hartree-Fock reference energy of the
active-space problem.}
\item{\code{num_particles}}{Length-2 integer vector
\code{c(alpha, beta)} — the number of spin-up and spin-down
electrons in the active space.}
\item{\code{num_spatial_orbitals}}{Number of active spatial
orbitals.}
\item{\verb{geometry, basis, charge, spin, mapper}}{Echoed inputs.}
}
}
\description{
End-to-end ab initio pipeline:
\enumerate{
\item A restricted Hartree-Fock reference is computed by \code{PySCFDriver}
in the requested atomic-orbital basis (STO-3G by default —
minimal, illustrative, and small enough for laptop-scale VQE).
\item Optionally, the chemical core orbitals are frozen out by
\code{FreezeCoreTransformer} — a cheap and orbital-space-neutral
reduction that typically halves the qubit count for first-row
organics.
\item The frontier orbitals are projected to an
\verb{(num_active_electrons, num_active_orbitals)} active space via
\code{ActiveSpaceTransformer}, keeping the chemistry local to the
coordination event the user cares about (e.g. Fe-O-C charge
transfer in an organo-mineral complex).
\item The resulting second-quantised Hamiltonian is mapped to qubits
by the chosen fermion-to-qubit transformation; by default the
\code{ParityMapper} is used with known particle numbers, which
taper off two qubits by Z2 symmetry reduction.
}
}
\details{
The returned object is a standard
\code{edaphos_quantum_hamiltonian} and can be dropped straight into
\code{\link[=quantum_vqe_fit]{quantum_vqe_fit()}} or \code{\link[=quantum_ibmq_submit]{quantum_ibmq_submit()}}. It additionally
exposes the full set of energy shifts as attributes so the user
can recompute the total molecular energy from a VQE result:
\deqn{E_\mathrm{total} = \langle H \rangle_\mathrm{VQE} +
                        E_\mathrm{shift}}
where
\eqn{E_\mathrm{shift} = E_\mathrm{nuc} +
                        E_\mathrm{frozen} +
                        E_\mathrm{active}}
sums the nuclear-repulsion constant, the frozen-core shift
(zero when \code{freeze_core = FALSE}) and the active-space
projection shift (zero when no active-space transformation is
applied). The helper \code{\link[=quantum_nature_total_energy]{quantum_nature_total_energy()}} performs
this reconstruction on a fit object.
}
\examples{
\dontrun{
  # H2 at equilibrium bond length, 2 qubits after parity tapering:
  h2 <- quantum_hamiltonian_from_pyscf(
    atom = "H 0 0 0; H 0 0 0.735",
    basis = "sto3g", charge = 0, spin = 0
  )
  fit <- quantum_vqe_fit(h2, seed = 1L)
  fit$energy + attr(h2, "nuclear_repulsion_energy")
}
}
\references{
Sun, Q. et al. (2018). PySCF: the Python-based simulations of
chemistry framework. \emph{WIREs Computational Molecular Science} \strong{8},
e1340.

Bravyi, S. et al. (2017). Tapering off qubits to simulate fermionic
Hamiltonians. arXiv:1701.08213.
}
\seealso{
\code{\link[=quantum_hamiltonian_organo_mineral_nature]{quantum_hamiltonian_organo_mineral_nature()}} for curated
presets; \code{\link[=quantum_vqe_fit]{quantum_vqe_fit()}} to minimise the returned
Hamiltonian.
}
