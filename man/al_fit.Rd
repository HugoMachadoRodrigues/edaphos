% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/active_fit.R
\name{al_fit}
\alias{al_fit}
\title{Fit a Quantile Regression Forest for Active Learning}
\usage{
al_fit(labeled, target, covariates, coords = NULL, num.trees = 500L, ...)
}
\arguments{
\item{labeled}{Data frame with observed target + covariates
(+ optional coordinates).}

\item{target}{Character, name of the target column.}

\item{covariates}{Character vector of covariate column names.}

\item{coords}{Optional length-2 character vector naming the x/y
coordinate columns. Required by the \code{"cost"} query strategy.}

\item{num.trees}{Integer, number of trees (default 500).}

\item{...}{Additional arguments forwarded to \code{ranger::ranger()}.}
}
\value{
A \code{edaphos_al_model} object.
}
\description{
Fits a Quantile Regression Forest (Meinshausen 2006) on the currently
labeled soil dataset, using \code{ranger::ranger(..., quantreg = TRUE)}. The
resulting object carries the data, model, and an initial history entry
and is the value on which \code{\link[=al_query]{al_query()}}, \code{\link[=al_update]{al_update()}} and
\code{\link[=al_loop]{al_loop()}} operate.
}
\examples{
\donttest{
  if (requireNamespace("sp", quietly = TRUE)) {
    data(meuse, package = "sp")
    m <- al_fit(
      labeled    = stats::na.omit(meuse[1:30, ]),
      target     = "lead",
      covariates = c("dist", "elev"),
      coords     = c("x", "y")
    )
    m
  }
}
}
\references{
Meinshausen N (2006). Quantile Regression Forests. \emph{Journal of Machine
Learning Research} 7, 983-999.
}
