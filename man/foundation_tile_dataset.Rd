% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/foundation_tiles.R
\name{foundation_tile_dataset}
\alias{foundation_tile_dataset}
\title{Build a lazy patch dataset over a \code{terra::SpatRaster}}
\usage{
foundation_tile_dataset(
  stack,
  patch_size = 16L,
  n_patches = 1000L,
  valid_mask = NULL,
  normalise = TRUE,
  seed = NULL
)
}
\arguments{
\item{stack}{A \code{terra::SpatRaster} with the channels to sample.}

\item{patch_size}{Integer -- spatial side of each patch.}

\item{n_patches}{Integer -- total number of patches the dataset
should expose. Sampling is with replacement when
\verb{n_patches > total valid cells}.}

\item{valid_mask}{Optional \code{terra::SpatRaster} of 0/1 defining
which cells are eligible as patch centres.}

\item{normalise}{Logical -- if \code{TRUE} (default) each channel is
standardised to zero mean / unit standard deviation based on the
raster's global statistics.}

\item{seed}{Optional integer for reproducibility.}
}
\value{
A list of class \code{edaphos_tile_dataset} with slots
\code{stack}, \code{patch_size}, \code{n_patches}, \code{n_channels}, \code{means},
\code{sds}, \code{valid_cells}; and a \code{sample(batch_size)} function that
returns a \verb{(batch_size, C, H, W)} R array.
}
\description{
Returns an \code{edaphos_tile_dataset} S3 object that knows how to sample
batched \verb{(B, C, H, W)} tensors from the underlying raster on demand
-- without ever loading the whole mosaic into memory. The dataset
is what \code{\link[=foundation_moco_pretrain_tiles]{foundation_moco_pretrain_tiles()}} consumes.
}
\details{
Patch centres are drawn uniformly within the AoI, optionally
constrained to a \code{valid_mask} (e.g. a land-sea mask or a biome
polygon). Each patch is a \verb{patch_size x patch_size} window
extracted from the raster; missing values are replaced by the
per-layer mean.
}
