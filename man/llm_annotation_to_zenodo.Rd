% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm_annotation.R
\name{llm_annotation_to_zenodo}
\alias{llm_annotation_to_zenodo}
\title{Package a reviewed gold-standard into a Zenodo-ready deposit bundle}
\usage{
llm_annotation_to_zenodo(
  reviewed_path,
  output_dir,
  title = "Cerrado gold-standard KG (edaphos)",
  authors = NULL,
  description = NULL,
  keywords = c("soil science", "pedometrics", "causal inference", "knowledge graph",
    "Cerrado"),
  license = "CC-BY-4.0",
  version = NULL,
  zip = TRUE
)
}
\arguments{
\item{reviewed_path}{Path to the reviewed JSONL.}

\item{output_dir}{Directory to create.  Will be made if it doesn't
exist.  Existing contents are overwritten.}

\item{title}{Deposit title (will appear on Zenodo).}

\item{authors}{Data frame with \code{family_name}, \code{given_name},
optional \code{orcid}, optional \code{affiliation} per row.  Defaults to a
single-author entry using \code{utils::maintainer()} on the package.}

\item{description}{Free-text description (HTML allowed).  Defaults
to a short summary including abstract / claim counts.}

\item{keywords}{Character vector of keyword tags.}

\item{license}{Licence identifier (default \code{"CC-BY-4.0"}).}

\item{version}{Optional version string to embed in metadata.json.}

\item{zip}{Logical; when \code{TRUE} (default), also produce a
\verb{<output_dir>.zip} alongside the directory.}
}
\value{
Invisibly, the path to the created directory.
}
\description{
Generates a self-contained directory (and optionally a zip archive)
that can be uploaded to Zenodo to mint a permanent DOI for the
gold-standard.  Bundle contents:
}
\details{
\itemize{
\item \code{gold_standard.jsonl}   -- cleaned gold-standard (drafts / rejected
removed, identical to
\code{\link[=llm_annotation_export]{llm_annotation_export()}} output).
\item \code{kg.ttl}                -- RDF 1.1 Turtle representation of the
aggregated KG built by treating each
accepted claim as a directed edge.
\item \code{metadata.json}         -- DataCite-compatible metadata ready for
the Zenodo REST API.  Also usable as
a source for a manual upload via the
Zenodo web UI.
\item \code{README.md}             -- Human-readable description with schema,
annotator, extractor, date, counts,
citation.
}

The function does NOT upload to Zenodo automatically (Zenodo requires
a personal access token which should never be hard-coded).  After the
bundle is built the user uploads the zip / files manually at
\url{https://zenodo.org/deposit/new} and records the minted DOI back
into the package's CITATION.cff / .zenodo.json.
}
