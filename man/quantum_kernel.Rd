% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantum_kernel.R
\name{quantum_kernel}
\alias{quantum_kernel}
\title{Quantum kernel Gram matrix via ZZFeatureMap overlap}
\usage{
quantum_kernel(X, Y = NULL, reps = 2L, backend = c("rcpp", "r"))
}
\arguments{
\item{X}{Numeric matrix or data frame (rows = samples, columns =
features). Features are mapped 1-1 to qubits; the number of
qubits equals \code{ncol(X)}. The current pure-R simulator scales to
about 8 qubits comfortably.}

\item{Y}{Optional numeric matrix / data frame with the same number
of columns as \code{X}. When \code{NULL} (default), the symmetric Gram
matrix \code{K(X, X)} is returned.}

\item{reps}{Integer encoding depth — forwarded to
\code{\link[=quantum_feature_map]{quantum_feature_map()}}.}

\item{backend}{One of \code{"rcpp"} (default, 10-50x faster; requires
the package to be properly installed so the compiled code is
available) or \code{"r"} (pure-R reference implementation, kept for
audit and fallback).}
}
\value{
Numeric matrix with \code{nrow(X)} rows and \code{nrow(Y \%||\% X)}
columns; all values lie in \verb{[0, 1]}.
}
\description{
Computes the Havlicek-et-al. quantum kernel
\deqn{K(\mathbf{x}_i, \mathbf{x}_j) \;=\;
       \bigl|\langle \phi(\mathbf{x}_j) \mid \phi(\mathbf{x}_i)\rangle\bigr|^2}
over one or two datasets whose rows are feature vectors in
\verb{[0, pi]} (see \code{\link[=quantum_scale]{quantum_scale()}}). The result is a positive
semi-definite matrix with ones on the diagonal.
}
\examples{
set.seed(1)
X <- matrix(runif(20, 0, pi), nrow = 5)
K <- quantum_kernel(X, reps = 2L)
stopifnot(isSymmetric(K))
stopifnot(all(abs(diag(K) - 1) < 1e-8))
}
\references{
Havlicek, V. et al. (2019). Supervised learning with
quantum-enhanced feature spaces. \emph{Nature} \strong{567}, 209-212.
}
