% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_iv.R
\name{causal_iv_fit_2sls}
\alias{causal_iv_fit_2sls}
\title{Two-stage least squares (2SLS) instrumental variable estimator}
\usage{
causal_iv_fit_2sls(data, exposure, outcome, instruments, covariates = NULL)
}
\arguments{
\item{data}{A data frame.}

\item{exposure}{Character; name of the endogenous exposure column.}

\item{outcome}{Character; name of the outcome column.}

\item{instruments}{Character vector; names of instrument columns.
More instruments than exposures gives an over-identified model
on which the Sargan test is applicable.}

\item{covariates}{Optional character vector of exogenous-control
column names included in both first and second stage.}
}
\value{
An \code{edaphos_causal_iv} object with components \code{effect},
\code{se}, \code{ci}, \code{stage1_F}, \code{stage1_R2}, \code{sargan_p} (NULL if exactly
identified), \code{n}, plus auxiliary fits for inspection.
}
\description{
Estimates the causal effect of an endogenous exposure \code{exposure} on
an outcome \code{outcome} using one or more instruments, optionally
conditional on exogenous controls.  Implements the classical 2SLS
estimator with the Wooldridge (2010) asymptotic variance -- the
naive \code{lm()} SE of the second stage is biased because it treats
the generated regressor as observed, a common mistake.
}
\details{
\subsection{Formal setup}{

Let
\itemize{
\item \code{X} be the endogenous exposure (one column),
\item \code{Z} the matrix of instruments,
\item \code{W} the matrix of exogenous controls (included in both stages).
}

Define the augmented matrices \verb{X_all = [W, X]} and
\verb{Z_all = [W, Z]} and the projection \verb{P = Z_all (Z_all' Z_all)^-1 Z_all'}.  The 2SLS estimator is

\deqn{\hat{\beta} = (X_{\text{all}}^\top P X_{\text{all}})^{-1} X_{\text{all}}^\top P Y}

with residuals computed using the ORIGINAL \code{X_all}, not the
first-stage fitted values, so that

\deqn{\hat{\sigma}^2 = (Y - X_{\text{all}} \hat{\beta})^\top (Y - X_{\text{all}} \hat{\beta}) / (n - k)}
\deqn{\widehat{\mathrm{Var}}(\hat{\beta}) = \hat{\sigma}^2 (X_{\text{all}}^\top P X_{\text{all}})^{-1}}

Identification conditions (Wooldridge 2010, section 5.1):
\itemize{
\item Relevance: \eqn{\mathrm{rank}(Z' X) = \dim(X)}; the first-stage
F-statistic should exceed 10 (Stock & Yogo 2005).
\item Exclusion: \eqn{Z} affects \eqn{Y} only through \eqn{X}.
\item Unconfoundedness: \eqn{Z \perp U} where \eqn{U} is any
unobserved confounder.
}
}
}
