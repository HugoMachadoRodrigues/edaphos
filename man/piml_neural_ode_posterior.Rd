% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/piml_posterior.R
\name{piml_neural_ode_posterior}
\alias{piml_neural_ode_posterior}
\title{Posterior predictive distribution from a Pillar 2 deep ensemble}
\usage{
piml_neural_ode_posterior(
  object,
  newdepths,
  include_obs_noise = FALSE,
  seed = NULL,
  units = NULL
)
}
\arguments{
\item{object}{An \code{edaphos_piml_neural_ode_ensemble}.}

\item{newdepths}{Numeric vector of depths at which to evaluate
the posterior predictive.}

\item{include_obs_noise}{Logical --- when \code{TRUE}, adds Gaussian
observation noise to every draw so the posterior is the
\emph{predictive distribution of a future observation} (aleatoric
included) rather than only the uncertainty on the mean
function. Default \code{FALSE}.}

\item{seed}{Optional RNG seed for the observation-noise draw.}

\item{units}{Optional units tag.}
}
\value{
An \code{edaphos_posterior} with \code{method = "ensemble"}.
}
\description{
Convenience wrapper that calls \code{\link[=predict.edaphos_piml_neural_ode_ensemble]{predict.edaphos_piml_neural_ode_ensemble()}}
on the requested depths and returns the result as an
\code{\link[=edaphos_posterior]{edaphos_posterior()}} with \code{query_type = "sample"} (one scalar
prediction per query depth) or \code{"feature"} (when the caller
passes a single depth and wants the posterior over that single
scalar).
}
