% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/foundation_finetune.R
\name{foundation_fit_regressor}
\alias{foundation_fit_regressor}
\title{Fine-tune or linearly probe a Pillar 4 encoder for regression}
\usage{
foundation_fit_regressor(
  encoder,
  x,
  y,
  freeze_backbone = TRUE,
  head = c("linear", "mlp"),
  hidden = c(64L, 32L),
  dropout = 0,
  epochs = 30L,
  batch_size = 32L,
  lr = 0.001,
  weight_decay = 0,
  backbone_lr_mult = 0.1,
  loss = c("mse", "huber"),
  val_split = 0.2,
  device = c("cpu", "mps", "cuda"),
  seed = NULL,
  verbose = FALSE
)
}
\arguments{
\item{encoder}{See \code{\link[=foundation_fit_classifier]{foundation_fit_classifier()}}.}

\item{x}{A 4-D array \verb{(N, C, H, W)} of patches.}

\item{y}{Numeric vector of length \code{N} with the target values.}

\item{freeze_backbone, head, hidden, dropout, epochs, batch_size, lr, weight_decay, backbone_lr_mult, val_split, device, seed, verbose}{See \code{\link[=foundation_fit_classifier]{foundation_fit_classifier()}}.}

\item{loss}{One of \code{"mse"} (default) or \code{"huber"}. Huber is more
robust to outlier pedons with extreme SOC / clay contents.}
}
\value{
An \code{edaphos_foundation_regressor} list with the same slots
as the classifier counterpart plus \code{y_mean}, \code{y_sd} (target
normalisation constants) and \code{val_rmse_history}.
}
\description{
Regression counterpart of \code{\link[=foundation_fit_classifier]{foundation_fit_classifier()}}. Attaches
a scalar-output head (linear or MLP) on top of a self-supervised
encoder and trains it against a numeric target \code{y}. Supports the
same two regimes — linear probing with a frozen backbone or full
fine-tuning with a two-group learning rate — and the same
\verb{device ∈ \{"cpu", "mps", "cuda"\}} dispatch.
}
\details{
Target normalisation is handled internally: \code{y} is centred and
scaled before training and un-scaled at \code{predict()} time so the
user never has to think about the numerical range of the head.
}
\examples{
\dontrun{
  moco <- foundation_weights_load("edaphos-cerrado-moco-v1")
  ds   <- readRDS("tools/pretrain/cerrado_dataset.rds")
  patches <- array(rnorm(300 * ds$n_channels * 16 * 16),
                   dim = c(300, ds$n_channels, 16, 16))
  soc <- rnorm(300, mean = 15, sd = 6)
  fit <- foundation_fit_regressor(
    moco, patches, soc,
    freeze_backbone = TRUE, head = "linear",
    epochs = 40L, device = "mps", seed = 1L
  )
  predict(fit, patches[1:10, , , , drop = FALSE])
}
}
\seealso{
\code{\link[=predict.edaphos_foundation_regressor]{predict.edaphos_foundation_regressor()}}.
}
