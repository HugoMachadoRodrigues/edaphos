% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/foundation_tiles.R
\name{foundation_tile_align}
\alias{foundation_tile_align}
\title{Align multiple raster sources onto a common analysis grid}
\usage{
foundation_tile_align(
  sources,
  target_res = 0.005,
  target_crs = "EPSG:4326",
  aoi = NULL,
  method = "bilinear"
)
}
\arguments{
\item{sources}{Named list of \code{terra::SpatRaster} objects.}

\item{target_res}{Numeric resolution of the output grid, in the
units of \code{target_crs}.}

\item{target_crs}{Character EPSG code of the output CRS (default
\code{"EPSG:4326"}).}

\item{aoi}{Optional AoI to crop after reprojection (see
\code{\link[=foundation_tile_source_soilgrids]{foundation_tile_source_soilgrids()}}).}

\item{method}{Resampling method for \code{terra::project()}. Defaults
to \code{"bilinear"} (good for continuous covariates).}
}
\value{
A single \code{terra::SpatRaster} with one layer per input
layer, aligned.
}
\description{
Projects every input \code{SpatRaster} onto a single template (common
CRS, common resolution, common extent), stacks them channel-wise,
and returns the unified mosaic. Designed to be robust to mixed
resolutions (SoilGrids at 250 m, WorldClim at 30 arc-sec, SRTM at
30 arc-sec, MODIS at 250 m, ERA5 at 0.1 degree) so MoCo v2
pretraining can consume a single multi-channel tensor.
}
