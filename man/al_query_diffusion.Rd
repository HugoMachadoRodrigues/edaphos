% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bridges_active_learning_v3.R
\name{al_query_diffusion}
\alias{al_query_diffusion}
\title{Diffusion-posterior-driven AL (Pilar 9 x Pilar 5)}
\usage{
al_query_diffusion(
  dm_fit,
  conditioning = NULL,
  n_samples = 16L,
  candidate_cells = NULL,
  n_select = 10L,
  combine = c("sd", "sd_x_mean_abs"),
  seed = NULL
)
}
\arguments{
\item{dm_fit}{An \code{edaphos_dm_fit} from \code{\link[=dm_fit]{dm_fit()}}.}

\item{conditioning}{Optional \verb{(n_samples, cond_dim)} matrix (one
row per posterior draw); when \code{NULL} the model is sampled
unconditionally.}

\item{n_samples}{Integer; number of posterior draws.}

\item{candidate_cells}{Optional integer matrix with columns \code{row},
\code{col} restricting the candidate pool to accessible sites.
Defaults to the full H x W grid.}

\item{n_select}{Integer; how many cells to return.}

\item{combine}{One of \code{"sd"} or \code{"sd_x_mean_abs"} to weight SD by
the absolute expected value (prioritises cells where the model
thinks the SOC is BOTH high and uncertain).}

\item{seed}{RNG seed.}
}
\value{
A data frame of class \code{edaphos_al_diffusion_query}
sorted by descending score.
}
\description{
Ranks candidate cells by the standard deviation of DDPM posterior
samples.  High-SD cells are the most uncertain map locations;
labelling there delivers the largest reduction in model entropy
per sample.
}
