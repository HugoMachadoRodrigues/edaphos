% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uncertainty.R
\name{edaphos_posterior}
\alias{edaphos_posterior}
\title{Unified posterior object for the edaphos pillars}
\usage{
edaphos_posterior(
  samples = NULL,
  mean = NULL,
  sd = NULL,
  epistemic_sd = NULL,
  aleatoric_sd = NULL,
  probs = c(0.05, 0.5, 0.95),
  method = c("ensemble", "bootstrap", "mcdropout", "bayesian", "loo_cv", "shots",
    "analytic", "gaussian"),
  query_type = c("effect", "param", "map", "feature", "sample", "energy", "other"),
  units = NULL,
  metadata = list(),
  n_samples_if_gaussian = 500L
)
}
\arguments{
\item{samples}{Optional numeric array or matrix. The first axis
is the posterior-draw axis of length \code{n_samples}; the remaining
axes are the query shape. A 1-D vector is treated as
\code{n_samples} scalar draws.}

\item{mean}{Optional numeric array with the query shape. If
\code{samples} is \code{NULL}, required together with \code{sd}.}

\item{sd}{Optional numeric array with the query shape.}

\item{epistemic_sd, aleatoric_sd}{Optional numeric arrays with the
query shape; the variance decomposition
\code{epistemic_sd^2 + aleatoric_sd^2 ~ sd^2} is expected to hold
approximately (exact for Gaussian posteriors).}

\item{probs}{Numeric vector of quantile probabilities to pre-
compute from \code{samples}. Defaults to \code{c(0.05, 0.5, 0.95)}.}

\item{method}{Character tag describing how the posterior was
produced, for diagnostics and plotting. One of \code{"ensemble"},
\code{"bootstrap"}, \code{"mcdropout"}, \code{"bayesian"}, \code{"loo_cv"},
\code{"shots"}, \code{"analytic"}, \code{"gaussian"}.}

\item{query_type}{Character tag describing what the posterior
is over. One of \code{"effect"}, \code{"param"}, \code{"map"}, \code{"feature"},
\code{"sample"}, \code{"energy"}, \code{"other"}.}

\item{units}{Optional free-text tag (e.g. "g/kg", "NDVI z-units").}

\item{metadata}{Optional list of extra provenance fields.}

\item{n_samples_if_gaussian}{If only \code{mean} and \code{sd} are provided,
the constructor synthesises this many Gaussian draws so that
downstream helpers (\code{uncertainty_calibrate}, \code{autoplot}) always
have a sample to work with. Defaults to \code{500L}.}
}
\value{
An \code{edaphos_posterior} object. See \code{?edaphos_posterior}
for the list of invariants the class satisfies.
}
\description{
Constructs an \code{edaphos_posterior} --- the single S3 class that
edaphos uses to represent predictive uncertainty regardless of
which pillar produced it. The constructor accepts either a
sample-based posterior (\code{samples}) or a Gaussian summary
(\code{mean} + \code{sd}), and derives the missing fields automatically.
}
\examples{
# From a posterior sample
post <- edaphos_posterior(
  samples    = matrix(stats::rnorm(200), nrow = 100, ncol = 2),
  method     = "bootstrap",
  query_type = "effect",
  units      = "g/kg per mm"
)
post

# From a Gaussian summary
post2 <- edaphos_posterior(
  mean       = c(0.5, 0.7),
  sd         = c(0.1, 0.2),
  method     = "gaussian",
  query_type = "param"
)
post2
}
