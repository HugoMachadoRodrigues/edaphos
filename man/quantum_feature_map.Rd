% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantum_kernel.R
\name{quantum_feature_map}
\alias{quantum_feature_map}
\title{Quantum feature map (Pillar 6)}
\usage{
quantum_feature_map(x, reps = 2L)
}
\arguments{
\item{x}{Numeric vector of features (one qubit per feature).}

\item{reps}{Integer, number of times the encoding circuit is
repeated (\verb{>= 1}). Higher values give more expressive kernels at
a per-sample simulation cost of \code{O(reps * 2^n)}.}
}
\value{
Complex vector of length \code{2^length(x)} — the state-vector
amplitudes of \eqn{\lvert\phi(\mathbf{x})\rangle} in the
computational basis.
}
\description{
Returns the complex amplitude vector of the ZZFeatureMap quantum
state \eqn{\lvert\phi(\mathbf{x})\rangle} produced by the data-
encoding circuit of Havlicek et al. (2019). The feature vector
\code{x} should already be normalised into the range \verb{[0, pi]} — see
\code{\link[=quantum_scale]{quantum_scale()}}.
}
\examples{
psi <- quantum_feature_map(c(pi/4, pi/3, pi/2), reps = 2L)
sum(Mod(psi)^2)  # 1 — quantum state is normalised
}
