% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm_benchmark.R
\name{llm_benchmark_match}
\alias{llm_benchmark_match}
\title{Match extracted LLM claims against a gold-standard set}
\usage{
llm_benchmark_match(predicted, gold, fuzzy = TRUE, fuzzy_threshold = 2L)
}
\arguments{
\item{predicted}{Data frame with columns \code{abstract_id}, \code{cause},
\code{effect} (and optional \code{confidence}). Every edge found by the
backend for a given abstract.}

\item{gold}{Data frame with columns \code{abstract_id}, \code{cause}, \code{effect}
(and optional \code{polarity}). One row per annotated claim.}

\item{fuzzy}{Logical; if \code{TRUE} (default), also count a predicted
edge as TP when one of its endpoints matches by Levenshtein
distance within \code{fuzzy_threshold}. If \code{FALSE}, require exact
match on canonicalised labels.}

\item{fuzzy_threshold}{Integer; maximum edit distance for a fuzzy
endpoint match. Default \code{2}.}
}
\value{
A data frame with columns \code{abstract_id}, \code{cause}, \code{effect},
\code{status} (one of \code{"tp"}, \code{"fp"}, \code{"fn"}), and \code{source} (either
\code{"predicted"} or \code{"gold"}).
}
\description{
Takes a data frame of extracted claims (from any backend) and a
data frame of gold-standard claims, and returns a per-claim match
table with TP / FP / FN labels.
}
\details{
Matching is done on the (cause, effect) pair after
canonicalisation.  A predicted claim is a \strong{true positive} if the
canonicalised pair appears in the gold set for the same abstract; a
\strong{false positive} if the pair is not in gold; gold entries not in
predictions are \strong{false negatives}.
}
