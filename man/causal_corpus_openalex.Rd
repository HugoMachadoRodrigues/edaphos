% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_corpus.R
\name{causal_corpus_openalex}
\alias{causal_corpus_openalex}
\title{Query the OpenAlex corpus}
\usage{
causal_corpus_openalex(
  query,
  max_results = 50L,
  from_year = NULL,
  to_year = NULL,
  mailto = NULL,
  timeout_sec = 120
)
}
\arguments{
\item{query}{Character search string.}

\item{max_results}{Integer cap on the number of works fetched
(max 200 per request).}

\item{from_year, to_year}{Optional integer year filters
(inclusive).}

\item{mailto}{Optional email string sent as \verb{mailto=} to identify
the client and unlock the "polite" rate-limit pool.}

\item{timeout_sec}{Request timeout (seconds).}
}
\value{
A data frame with columns \code{source}, \code{title}, \code{abstract},
\code{year}, \code{doi}, \code{url}.
}
\description{
Thin wrapper around the \strong{OpenAlex Works API} (no account needed;
a courtesy \verb{mailto=} parameter raises the user's rate-limit tier).
OpenAlex stores abstracts as an inverted index; they are
reconstructed to plain text here so the result is
LLM-extractor-ready.
}
\examples{
\dontrun{
  oa <- causal_corpus_openalex("Cerrado soil organic carbon",
                                max_results = 25,
                                mailto = "you@example.org")
  head(oa[, c("source", "title", "year")])
}
}
