% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_kg_io.R
\name{causal_kg_to_turtle}
\alias{causal_kg_to_turtle}
\title{Export a Knowledge Graph to RDF 1.1 Turtle}
\usage{
causal_kg_to_turtle(
  kg,
  path = NULL,
  base_uri = "https://edaphos.io/kg/",
  schema_uri = "https://edaphos.io/schema#",
  namespaces = character(0),
  include_metadata = TRUE
)
}
\arguments{
\item{kg}{An \code{edaphos_causal_kg}.}

\item{path}{Optional output file path. When \code{NULL} (default), the
Turtle document is returned as a single character string.}

\item{base_uri}{Character — base IRI for the KG. Must end with
\code{"/"} or \code{"#"}. Default \code{"https://edaphos.io/kg/"}.}

\item{schema_uri}{Character — IRI of the \verb{eds:} schema namespace.
Default \code{"https://edaphos.io/schema#"}.}

\item{namespaces}{Named character vector of extra prefix bindings
to declare. Useful when you want to reference external
vocabularies (e.g. \code{c(agrovoc = "http://aims.fao.org/aos/agrovoc/")}).}

\item{include_metadata}{Logical — emit document-level metadata
(creation time, \code{edaphos} version) as \verb{prov:} statements.
Default \code{TRUE}.}
}
\value{
When \code{path} is \code{NULL}, invisibly returns the Turtle
document as a length-1 character vector. When \code{path} is given,
writes the document to disk and invisibly returns \code{path}.
}
\description{
Serialises an \code{edaphos_causal_kg} as \strong{RDF 1.1 Turtle}
(\url{https://www.w3.org/TR/turtle/}), the W3C's canonical
human-readable triple language. Each edge becomes a reified
\code{rdf:Statement} so confidence, evidence, source and timestamp are
preserved losslessly; each node is given a stable IRI inside a
user-controlled namespace. The emitter is written in pure R with
no external dependency — the output is guaranteed parseable by any
RDF 1.1-conformant consumer (rdflib, Jena, Virtuoso, Blazegraph,
GraphDB, Oxigraph …).
}
\details{
The emitted graph uses the following prefixes by default (all
overridable via \code{namespaces}):
\itemize{
\item \verb{ed:}   — edaphos KG namespace (one IRI per node and edge).
\item \verb{eds:}  — edaphos schema namespace (defines \code{eds:Causes},
\code{eds:confidence}, \code{eds:evidence}, \code{eds:source}).
\item \verb{rdf:}, \verb{rdfs:}, \verb{xsd:}, \verb{prov:}, \verb{dct:} — standard W3C
vocabularies.
}

Every node \code{L} becomes an IRI
\verb{<base_uri>node/<sanitised_L>}; every edge becomes an IRI
\verb{<base_uri>edge/<cause>__<effect>} typed as \code{rdf:Statement}, with
\code{rdf:subject} / \code{rdf:predicate} / \code{rdf:object} pointing at the
nodes and \code{eds:Causes} used as the predicate for the causal
direction. Provenance is attached via \code{dct:source}
(bibliographic identifier) and \code{prov:generatedAtTime} (timestamp).
}
\examples{
\donttest{
  kg <- causal_kg_new()
  kg <- causal_kg_add_edge(kg, "precipitation", "soc",
                            source = "Jenny 1941",
                            evidence = "Higher precipitation favours SOC.",
                            confidence = 0.9)
  ttl <- causal_kg_to_turtle(kg)
  substr(ttl, 1, 200)

  # Write to disk and (optionally) round-trip through rdflib if
  # installed -- a useful SPARQL-queryable artefact.
  tf <- tempfile(fileext = ".ttl")
  causal_kg_to_turtle(kg, tf)
}
}
\references{
Beckett, D. (2014). RDF 1.1 Turtle — Terse RDF Triple Language.
\emph{W3C Recommendation}.
}
\seealso{
\code{\link[=causal_kg_save]{causal_kg_save()}} for binary RDS persistence.
}
