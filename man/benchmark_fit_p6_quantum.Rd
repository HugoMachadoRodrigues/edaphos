% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark_wosis.R
\name{benchmark_fit_p6_quantum}
\alias{benchmark_fit_p6_quantum}
\title{Benchmark wrapper: Pilar 6 -- bootstrap-ensembled quantum KRR}
\usage{
benchmark_fit_p6_quantum(
  train,
  test,
  cov_cols,
  n_pcs = 6L,
  reps = 2L,
  lambda = 0.5,
  n_boot = 20L,
  seed = 1L,
  calibrate = TRUE
)
}
\arguments{
\item{train, test}{Data frames with \code{soc} + \code{cov_cols}.}

\item{cov_cols}{Character vector of covariate column names.}

\item{n_pcs}{Integer; number of PCs (= qubits).  Default \code{6L}.}

\item{reps}{Integer; ZZFeatureMap repetitions.  Default \code{2L}.}

\item{lambda}{Ridge regulariser.  Default \code{0.5}.}

\item{n_boot}{Integer; number of bootstrap KRR fits.  Default \code{20L}.}

\item{seed}{Optional RNG seed.}

\item{calibrate}{Logical (default \code{TRUE}).  When \code{TRUE}, residual
noise from a full-data quantum-KRR fit is injected into every
posterior sample (see Pilar-1 wrapper for the rationale).}
}
\value{
An \code{edaphos_posterior} with
\code{method = "ensemble"}, \code{query_type = "map"}.
}
\description{
PCA-reduces the covariate matrix to \code{n_pcs} components rescaled to
\verb{[-pi, pi]}, then trains \code{n_boot} quantum-kernel ridge regressors
on bootstrap resamples and aggregates their predictions into a
predictive posterior.
}
