% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_active_learning.R
\name{al_query_causal}
\alias{al_query_causal}
\title{Causal Active Learning: query the next sample(s) that most reduce
the uncertainty of a targeted causal effect}
\usage{
al_query_causal(
  data,
  pool,
  dag,
  exposure,
  outcome,
  adjustment = NULL,
  n_select = 5L,
  strategy = c("leverage", "bootstrap"),
  B = 200L,
  seed = NULL
)
}
\arguments{
\item{data}{Data frame with the exposure, outcome and adjustment
columns (the current labelled set).}

\item{pool}{Data frame with the same columns as \code{data}; the
unlabelled candidate pool to query.}

\item{dag}{A \code{dagitty} DAG.}

\item{exposure, outcome}{Character; column names of the causal query.}

\item{adjustment}{Optional character vector of adjustment-set
columns.  Inferred from \code{dag} via \code{\link[=causal_adjustment_set]{causal_adjustment_set()}}
when \code{NULL}.}

\item{n_select}{Integer; how many candidates to return.  Default
\code{5L}.}

\item{strategy}{One of \code{"leverage"} (hat-matrix diagonal,
closed-form, fast) or \code{"bootstrap"} (re-bootstrap the effect for
each candidate; slow but exact under the linear-OLS estimator).}

\item{B}{Bootstrap replications when \code{strategy = "bootstrap"}.}

\item{seed}{RNG seed for bootstrap reproducibility.}
}
\value{
A data frame with one row per candidate, columns
\code{pool_index}, \code{leverage}, \code{expected_var_reduction}, sorted by
descending expected reduction.  The top-\code{n_select} rows are the
recommended next samples.
}
\description{
Closes the loop between Pillar 1 (causal identification via
backdoor adjustment) and Pillar 5 (autonomous active learning):
instead of choosing candidates by marginal-Y uncertainty (classical
AL), we choose by expected shrinkage of \verb{Var(beta_hat_\{X->Y\})}.
}
