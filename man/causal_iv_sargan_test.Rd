% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_iv.R
\name{causal_iv_sargan_test}
\alias{causal_iv_sargan_test}
\title{Sargan test for instrument over-identification}
\usage{
causal_iv_sargan_test(data, exposure, outcome, instruments, covariates = NULL)
}
\arguments{
\item{data}{A data frame.}

\item{exposure}{Character; name of the endogenous exposure column.}

\item{outcome}{Character; name of the outcome column.}

\item{instruments}{Character vector; names of instrument columns.
More instruments than exposures gives an over-identified model
on which the Sargan test is applicable.}

\item{covariates}{Optional character vector of exogenous-control
column names included in both first and second stage.}
}
\value{
Named list with \code{stat}, \code{df}, \code{p}.
}
\description{
When the model is over-identified (more instruments than
endogenous exposures), the Sargan (1958) J-statistic tests the
null hypothesis that all instruments are valid (i.e., the
exclusion restriction holds).  The statistic is
}
\details{
\deqn{J = n \cdot R^2_{uu}}

where \verb{R^2_uu} is the R-squared of regressing the 2SLS residuals
on all instruments (and controls).  Under H0, \code{J ~ chi-sq(L - K)}
where \code{L} is the number of instruments and \code{K} is the number of
endogenous regressors.  Rejection (p < 0.05) is evidence that at
least one instrument is invalid.
}
