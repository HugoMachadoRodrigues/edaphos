% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal_posterior.R
\name{temporal_convlstm_mcdropout_predict}
\alias{temporal_convlstm_mcdropout_predict}
\title{MC-dropout predictive draws from a ConvLSTM fit}
\usage{
temporal_convlstm_mcdropout_predict(
  object,
  sequence,
  n_draws = 50L,
  return_sequence = NULL,
  seed = NULL
)
}
\arguments{
\item{object}{An \code{edaphos_temporal_convlstm} whose underlying
\code{nn_module} contains \code{nn_dropout2d} layers. Fits produced by
\code{\link[=temporal_convlstm_fit]{temporal_convlstm_fit()}} with the default \code{dropout_p = 0} work
but produce a degenerate constant draw.}

\item{sequence}{5-D input tensor \verb{(batch, T, C, H, W)} or equivalent
R array.}

\item{n_draws}{Integer; number of MC forward passes.}

\item{return_sequence}{Logical override; defaults to the value used
at training time.}

\item{seed}{Optional integer seed.}
}
\value{
A numeric array; first axis is the draw axis. Shape:
\verb{(n_draws, batch, T, H, W)} when \code{return_sequence = TRUE}, else
\verb{(n_draws, batch, H, W)}.
}
\description{
Runs \code{n_draws} forward passes through a fitted \code{edaphos_temporal_convlstm}
with dropout active in train mode, producing a Monte-Carlo sample of
the predictive posterior (Gal and Ghahramani 2016). The fit must
have been trained with \code{dropout_p > 0} for this to give a
non-degenerate sample; when the fit has no dropout layers the draws
collapse to the deterministic forward pass.
}
\references{
Gal, Y. and Ghahramani, Z. (2016). Dropout as a Bayesian
approximation: representing model uncertainty in deep learning.
\emph{ICML 33}, 1050-1059.
}
