% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantum_foundation.R
\name{qf_embed_reduce}
\alias{qf_embed_reduce}
\title{Reduce foundation-model embeddings to PCs and scale to quantum range}
\usage{
qf_embed_reduce(embeddings, n_pcs = 8L)
}
\arguments{
\item{embeddings}{Numeric matrix (obs x embedding-dim).}

\item{n_pcs}{Integer; number of PCs to retain.  Default \code{8L}
(the largest \code{n} for which \code{quantum_kernel()} stays classically
simulable on a laptop).}
}
\value{
A list with components \code{X_q} (the PCs in \verb{[-pi, pi]},
ready for \code{quantum_krr_fit()}), \code{rotation}, \code{variance_explained},
\code{pca_center}, \code{pca_scale}, and \code{range_min}/\code{range_max} used for
the pi-rescale.
}
\description{
Takes a matrix of per-observation foundation embeddings and returns
the top-\code{n_pcs} principal components rescaled into \verb{[-pi, pi]},
the natural input range of the ZZFeatureMap.  Zero-variance columns
are dropped before PCA.  The rotation and scaling are stored on the
return object so new observations can be projected identically.
}
