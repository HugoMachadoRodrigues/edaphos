% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal_convlstm.R
\name{temporal_convlstm_rollout}
\alias{temporal_convlstm_rollout}
\title{Multi-step rollout forecast}
\usage{
temporal_convlstm_rollout(object, past_sequence, future_drivers)
}
\arguments{
\item{object}{A \code{edaphos_temporal_convlstm} trained with
\code{return_sequence = TRUE}.}

\item{past_sequence}{Array \verb{(batch, T_past, C, H, W)} — the observed
window used for state warm-up.}

\item{future_drivers}{Array \verb{(batch, T_future, C, H, W)} with the
same channel layout as \code{past_sequence}.}
}
\value{
Array \verb{(batch, T_future, H, W)} with per-step predictions.
}
\description{
When future driver channels are known (e.g. climate forecasts,
calendar-based covariates, planned irrigation), a ConvLSTM trained
with \code{return_sequence = TRUE} can simply be re-applied to a longer
sequence covering \strong{past + future} time steps — the hidden state
propagates the soil memory, and every step gets its own prediction.
This function automates that call and returns only the future part
of the prediction.
}
