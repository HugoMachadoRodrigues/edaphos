% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/foundation_moco.R
\name{foundation_moco_embed_raster}
\alias{foundation_moco_embed_raster}
\title{Apply a fitted MoCo v2 encoder over a full raster mosaic}
\usage{
foundation_moco_embed_raster(
  moco,
  stack,
  dataset,
  patch_size = NULL,
  stride = NULL,
  projection = FALSE
)
}
\arguments{
\item{moco}{An \code{edaphos_foundation_moco} returned by
\code{\link[=foundation_moco_pretrain_tiles]{foundation_moco_pretrain_tiles()}}.}

\item{stack}{A \code{terra::SpatRaster} with the \strong{same channel order}
as the one used at training time.}

\item{dataset}{The \code{edaphos_tile_dataset} used at training time
(provides the per-channel normalisation statistics).}

\item{patch_size}{Integer -- must equal \code{dataset$patch_size}.}

\item{stride}{Integer step size of the sliding window (default
half of \code{patch_size}).}

\item{projection}{Logical -- if \code{TRUE} return the L2-normalised
projection-head outputs instead of the backbone features.}
}
\value{
A \code{terra::SpatRaster} with \code{feature_dim} (or \code{proj_dim})
layers.
}
\description{
Slides a \verb{patch_size x patch_size} window over the input raster at
stride \code{stride}, extracts each patch, encodes it with the
backbone of \code{moco$encoder_q}, and writes the resulting embedding
vector back as a multi-layer \code{terra::SpatRaster} whose layer count
equals \code{moco$feature_dim}. Patches whose centre cell is NA in the
input produce an NA row in the output.
}
\details{
Normalisation uses the global per-layer mean and sd stored in
\code{dataset}, so the embedding pipeline is consistent between
pretraining and inference.
}
