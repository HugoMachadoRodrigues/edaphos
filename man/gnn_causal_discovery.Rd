% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bridges_structure_v3.R
\name{gnn_causal_discovery}
\alias{gnn_causal_discovery}
\title{Graph-based causal discovery (Pilar 10 x Pilar 1)}
\usage{
gnn_causal_discovery(
  gnn_fit,
  feature_frame,
  method = c("hc", "tabu", "pc-stable"),
  whitelist = NULL,
  blacklist = NULL,
  n_emb_cols = NULL,
  bootstrap = FALSE,
  R_boot = 100L,
  seed = NULL
)
}
\arguments{
\item{gnn_fit}{An \code{edaphos_gnn_gat} from \code{\link[=gnn_fit]{gnn_fit()}}.}

\item{feature_frame}{Data frame of the actual variables to form
the DAG over.  Must have the same number of rows as the graph
on which \code{gnn_fit} was trained.}

\item{method}{Passed to \code{\link[=causal_structure_learn]{causal_structure_learn()}}; default
\code{"hc"}.}

\item{whitelist, blacklist}{Optional edge constraints (see
\code{\link[=causal_structure_learn]{causal_structure_learn()}}).}

\item{n_emb_cols}{Integer; how many GAT embedding dimensions to
use as conditioners.  Default \code{min(8, emb_dim)} to keep the
search space manageable.}

\item{bootstrap}{Logical; pass to \code{causal_structure_learn()}.}

\item{R_boot}{Integer; bootstrap resamples.}

\item{seed}{Optional RNG seed.}
}
\value{
An \code{edaphos_causal_kg} from the underlying structure-learn
call, restricted to edges between variables in \code{feature_frame}.
}
\description{
Runs \code{causal_structure_learn()} on a feature frame augmented with
GAT node embeddings.  Returns the discovered DAG restricted to the
user's canonical variables; embeddings act as nuisance
conditioners that absorb spatial dependence the expert DAG does
not name.
}
