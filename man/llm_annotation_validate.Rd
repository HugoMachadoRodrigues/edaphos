% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm_annotation.R
\name{llm_annotation_validate}
\alias{llm_annotation_validate}
\title{Validate a gold-standard JSONL file}
\usage{
llm_annotation_validate(path, strict_vocab = TRUE)
}
\arguments{
\item{path}{Path to the JSONL file.}

\item{strict_vocab}{Logical; if \code{TRUE} (default), flag any \code{cause}
or \code{effect} outside \code{\link[=llm_annotation_vocabulary]{llm_annotation_vocabulary()}}.}
}
\value{
Named list with \code{ok} (logical), \code{errors} (character vector)
and \code{summary} (data frame with per-record counts).
}
\description{
Checks every record for required fields, every claim for required
fields, that \code{cause} / \code{effect} belong to the canonical vocabulary
(unless \code{strict_vocab = FALSE}), that \code{polarity} is \code{"+"} or
\code{"-"}, and that \code{confidence} is in \verb{[0, 1]}.
}
