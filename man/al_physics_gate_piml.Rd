% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/active_physics_gate.R
\name{al_physics_gate_piml}
\alias{al_physics_gate_piml}
\title{Build a physics gate from a PIML profile fit}
\usage{
al_physics_gate_piml(
  profile_fit,
  safety_factor = 1.2,
  lower = NULL,
  upper = NULL
)
}
\arguments{
\item{profile_fit}{A \code{edaphos_piml_profile} or
\code{edaphos_piml_neural_ode} object.}

\item{safety_factor}{Numeric (>= 1). 1 = strict envelope; 1.2 = allow
20 \% slack on each side.}

\item{lower}{Optional hard lower bound in the target units
(e.g. \code{0} for mass fractions). If supplied, overrides the lower
end of the envelope.}

\item{upper}{Optional hard upper bound.}
}
\value{
A function \verb{function(candidates, predicted_mean)} suitable for
\verb{al_query(..., physics_gate = <this>)}.
}
\description{
Convenience helper for constructing the \code{physics_gate} argument of
\code{\link[=al_query]{al_query()}} and \code{\link[=al_loop]{al_loop()}} from an existing PIML fit — either the
parametric profile (\code{\link[=piml_profile_fit]{piml_profile_fit()}}) or the Neural ODE variant
(\code{\link[=piml_neural_ode_fit]{piml_neural_ode_fit()}}).
}
\details{
The gate rejects any candidate whose model-predicted target value falls
outside a physically plausible envelope derived from the PIML fit. For
the parametric ODE the envelope is
\verb{[min(y0, y_inf), max(y0, y_inf)]}; for the Neural ODE it is the
range of predictions between \code{z = 0} and the deepest training depth
(extrapolated to \code{2 * max(depth)} as a conservative floor). Both are
widened by \code{safety_factor}.
}
