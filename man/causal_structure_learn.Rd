% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_structure.R
\name{causal_structure_learn}
\alias{causal_structure_learn}
\title{Structure learning from horizon data -> Knowledge Graph}
\usage{
causal_structure_learn(
  data,
  variables = NULL,
  method = c("hc", "tabu", "pc-stable", "mmhc"),
  whitelist = NULL,
  blacklist = NULL,
  score = "bic-g",
  alpha = 0.05,
  bootstrap = FALSE,
  R_boot = 200L,
  seed = NULL,
  verbose = FALSE
)
}
\arguments{
\item{data}{A data frame with one row per observation (typically a
pedon or horizon).}

\item{variables}{Optional character vector of columns to include
in the analysis. When \code{NULL}, uses every numeric column of
\code{data}.}

\item{method}{One of \code{"hc"} (default), \code{"tabu"}, \code{"pc-stable"},
\code{"mmhc"}.}

\item{whitelist}{Optional data frame with columns \code{from} and \code{to}
listing edges that \emph{must} be present in the learned DAG. Useful
for pedological priors, e.g. "parent material must precede soil
chemistry".}

\item{blacklist}{Optional data frame with columns \code{from} and \code{to}
listing edges that \emph{must not} appear — typically the reverse of
the whitelist plus any physically impossible arrows (e.g.
\code{soc -> elevation}).}

\item{score}{Scoring function for score-based algorithms
(\code{"hc"}, \code{"tabu"}, \code{"mmhc"}). Defaults to \code{"bic-g"} (Gaussian
BIC) for continuous data; \code{"bge"} (Bayesian Gaussian
equivalent) is the alternative. For discrete data use \code{"bic"}
or \code{"bde"}.}

\item{alpha}{Significance level for conditional-independence
tests in \code{"pc-stable"} / \code{"mmhc"}. Default \code{0.05}.}

\item{bootstrap}{Logical — run a bootstrap to estimate edge
confidence. Default \code{FALSE}.}

\item{R_boot}{Integer — number of bootstrap replicates. Default
\code{200}.}

\item{seed}{Optional integer — used by the bootstrap resampler.}

\item{verbose}{Logical — forwarded to \code{bnlearn}.}
}
\value{
An \code{edaphos_causal_kg} whose \code{source} field on every edge
reads \code{"structure_learn(method=...)"} and whose \code{confidence} is
either \code{1.0} (point learned DAG, no bootstrap) or the bootstrap
edge-frequency.
}
\description{
Learns a Directed Acyclic Graph (DAG) over a set of soil covariate
/ response variables directly from a horizon-level data frame,
using one of four canonical structure-learning algorithms from the
\code{bnlearn} package (Scutari 2010). The returned object is an
\code{edaphos_causal_kg} so the learned DAG can be (i) unioned with the
LLM-extracted Knowledge Graph via \code{\link[=causal_augment_dag]{causal_augment_dag()}}, (ii)
exported to RDF via \code{\link[=causal_kg_to_turtle]{causal_kg_to_turtle()}}, and (iii) consumed by
the backdoor-adjustment estimator via \code{\link[=causal_kg_to_dagitty]{causal_kg_to_dagitty()}}.
}
\section{Algorithms}{

\describe{
\item{\code{"hc"} (default)}{Hill-climbing greedy search over DAG
space maximising a Bayesian Information Criterion (BIC) score
(Gaussian BIC for continuous variables).
Deterministic, fast, and widely used.}
\item{\code{"tabu"}}{Tabu-search variant that escapes local optima by
keeping a short memory of recently visited DAGs.}
\item{\code{"pc-stable"}}{PC-stable constraint-based algorithm
(Colombo and Maathuis 2014). Starts from a complete skeleton
and removes edges based on partial correlation tests; returns
a CPDAG which we extend to a DAG via a topological order
consistent with the whitelist.}
\item{\code{"mmhc"}}{Max-Min Hill-Climbing (Tsamardinos, Brown and
Aliferis 2006). Hybrid: learns a skeleton by constraint tests,
then orients edges by hill-climbing over a BIC score.}
}
}

\section{Bootstrap edge confidence}{

When \code{bootstrap = TRUE}, a non-parametric bootstrap over rows of
\code{data} is performed. Each bootstrap replicate runs the same
\code{method}, whitelist and blacklist; the fraction of replicates in
which each edge appears is recorded as that edge's \code{confidence} in
the returned KG. This gives an honest uncertainty estimate on the
learned structure, useful when the sample size is modest relative
to the number of variables (a common situation for soil surveys).
}

\examples{
\dontrun{
  data(br_cerrado)
  kg_learned <- causal_structure_learn(
    br_cerrado,
    variables = c("elev", "slope", "twi", "map_mm", "ndvi", "soc"),
    method    = "hc",
    whitelist = data.frame(from = c("elev", "map_mm"),
                            to   = c("twi",  "soc")),
    bootstrap = TRUE, R_boot = 200L, seed = 1L
  )
  print(kg_learned)
}
}
\references{
Spirtes, P., Glymour, C. and Scheines, R. (2000). \emph{Causation,
Prediction, and Search} (2nd ed.). MIT Press.

Scutari, M. (2010). Learning Bayesian networks with the \code{bnlearn}
R package. \emph{Journal of Statistical Software} \strong{35}, 1–22.

Colombo, D. and Maathuis, M. H. (2014). Order-independent
constraint-based causal structure learning. \emph{Journal of Machine
Learning Research} \strong{15}, 3741–3782.

Tsamardinos, I., Brown, L. E. and Aliferis, C. F. (2006). The
max-min hill-climbing Bayesian network structure learning
algorithm. \emph{Machine Learning} \strong{65}, 31–78.
}
\seealso{
\code{\link[=causal_kg_new]{causal_kg_new()}}, \code{\link[=causal_augment_dag]{causal_augment_dag()}},
\code{\link[=causal_kg_to_dagitty]{causal_kg_to_dagitty()}}, \code{\link[=causal_kg_to_turtle]{causal_kg_to_turtle()}}.
}
