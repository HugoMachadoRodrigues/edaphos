% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/active_posterior.R
\name{active_learning_posterior}
\alias{active_learning_posterior}
\title{Posterior predictive distribution of an edaphos Active-Learning fit}
\usage{
active_learning_posterior(model, newdata, n_quantiles = 99L, units = NULL)
}
\arguments{
\item{model}{An \code{edaphos_al_model} produced by \code{\link[=al_fit]{al_fit()}}.}

\item{newdata}{A data frame with (at least) the columns used as
covariates at fit time.}

\item{n_quantiles}{Integer; size of the equally-spaced grid of
quantiles to request from the QRF. Defaults to \code{99L} (1 \% to
99 \% in 1 \% steps, which is a reasonable trade-off between a
smooth empirical CDF and \code{ranger::predict()} cost).}

\item{units}{Optional free-text units tag.}
}
\value{
An \code{edaphos_posterior} with \code{method = "ensemble"} (the QRF
conditional distribution being itself an ensemble over tree
leaves) and \code{query_type = "sample"}.
}
\description{
Samples the conditional distribution of the target at every row of
\code{candidates} by asking the underlying Quantile Regression Forest
(ranger) for a grid of quantiles. The returned
\code{edaphos_posterior} carries those quantile samples directly, so
\code{\link[=uncertainty_calibrate]{uncertainty_calibrate()}} and \code{\link[ggplot2:autoplot]{ggplot2::autoplot()}} work without changes.
}
\references{
Meinshausen, N. (2006). Quantile regression forests. \emph{Journal of
Machine Learning Research} \strong{7}, 983-999.
}
