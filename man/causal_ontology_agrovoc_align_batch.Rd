% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_ontology.R
\name{causal_ontology_agrovoc_align_batch}
\alias{causal_ontology_agrovoc_align_batch}
\title{Concurrent AGROVOC alignment for a large vocabulary}
\usage{
causal_ontology_agrovoc_align_batch(
  terms,
  cache_path = NULL,
  max_active = 5L,
  max_per_term = 5L,
  max_retries = 2L,
  endpoint = "https://agrovoc.fao.org/sparql",
  timeout_sec = 60L,
  verbose = FALSE
)
}
\arguments{
\item{terms}{Character vector of free-text terms.}

\item{cache_path}{Optional \code{.rds} file path. When supplied, the
function reads cached matches on entry and writes updated
matches on exit.}

\item{max_active}{Integer — maximum number of concurrent HTTP
connections. Default \code{5}; raise for faster throughput, lower if
the endpoint is rate-limiting you. \code{httr2} transparently pools
up to \code{max_active} connections.}

\item{max_per_term}{Integer — AGROVOC query \code{LIMIT} per term; the
closest label by Levenshtein distance is retained.}

\item{max_retries}{Integer — number of retry rounds for terms that
come back with a non-200 response. Retries apply exponential
backoff (\code{2 ^ attempt} seconds between rounds).}

\item{endpoint}{AGROVOC SPARQL endpoint URL. Override for a mirror
or a local SPARQL proxy.}

\item{timeout_sec}{Per-request timeout (seconds).}

\item{verbose}{Logical — print a one-line progress summary after
each parallel round.}
}
\value{
A data frame with columns \code{term}, \code{uri}, \code{label},
\code{distance} (NA when no hit). Unresolved terms keep NA slots; the
caller can decide whether to retry them or accept a partial
alignment.
}
\description{
Resolves a vector of free-text \code{terms} against the FAO AGROVOC
SPARQL endpoint using \strong{parallel HTTP dispatch}, an on-disk cache,
and retry logic with exponential backoff. Designed for Knowledge
Graphs built from thousands of papers, where the per-term overhead
of \code{\link[=causal_ontology_agrovoc_align]{causal_ontology_agrovoc_align()}} (~5–10 s / term against
agrovoc.fao.org) dominates the runtime.
}
\section{SPARQL-level vs transport-level batching}{

A single composite SPARQL query that binds N terms via \code{VALUES}
and filters labels with \code{CONTAINS(?label, ?term)} is the
theoretically optimal batching strategy, but AGROVOC's production
endpoint consistently rejects such queries with a 504 gateway
timeout because the substring predicate cannot short-circuit
against the bound term set. \code{causal_ontology_agrovoc_align_batch()}
therefore batches at the transport layer instead: it issues one
single-term query per uncached input and dispatches them through
\code{httr2::req_perform_parallel()} with \code{max_active} concurrent
connections. The on-wire semantics are identical to
\code{\link[=causal_ontology_agrovoc_align]{causal_ontology_agrovoc_align()}}; only the wall-clock time
changes.
}

\section{Caching and resumability}{

The cache is a named list keyed by normalised term, persisted as a
single \code{.rds} file. On entry every cached term is short-circuited
without a network call. Failed terms are \strong{not} cached, so a
resumed run retries them; successful terms become permanent.
Pointing a fresh \code{cache_path} at an existing \code{.rds} keeps the
cache; passing \code{NULL} disables persistence (in-memory only).
}

\examples{
\dontrun{
  # 100-term vocabulary extracted from a KG built over 10k abstracts.
  vocab <- unique(c(causal_kg_edges(kg)$cause,
                     causal_kg_edges(kg)$effect))
  ag <- causal_ontology_agrovoc_align_batch(
    vocab,
    cache_path = "tools/.agrovoc_cache.rds",
    max_active = 8L,
    max_retries = 2L,
    verbose = TRUE
  )
  # Resolution rate:
  mean(!is.na(ag$uri))
}
}
\seealso{
\code{\link[=causal_ontology_agrovoc_align]{causal_ontology_agrovoc_align()}} for the sequential
variant; \code{\link[=causal_kg_alignment]{causal_kg_alignment()}} for KG-level dispatch.
}
