% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_iv.R
\name{causal_iv_from_embeddings}
\alias{causal_iv_from_embeddings}
\title{Fit 2SLS using foundation-model (or proxy) embeddings as instruments}
\usage{
causal_iv_from_embeddings(
  data,
  embeddings,
  exposure,
  outcome,
  covariates = NULL,
  n_pcs = 5L
)
}
\arguments{
\item{data}{Data frame with \code{exposure}, \code{outcome} and any
\code{covariates}.}

\item{embeddings}{Numeric matrix with \code{nrow(data)} rows (one per
data row) and any number of columns (embedding dimensions).}

\item{exposure, outcome, covariates}{See \code{\link[=causal_iv_fit_2sls]{causal_iv_fit_2sls()}}.}

\item{n_pcs}{Integer; number of top principal components to keep
as instruments.  Default \code{5L}.}
}
\value{
\code{edaphos_causal_iv} object (see \code{\link[=causal_iv_fit_2sls]{causal_iv_fit_2sls()}}).
}
\description{
Convenience wrapper that:
\enumerate{
\item Takes a matrix of per-profile embeddings (rows = profiles,
columns = embedding dims),
\item Reduces them to \code{n_pcs} principal components,
\item Attaches the PCs to \code{data} as new columns named \code{PC_1}, ...,
\item Runs \code{\link[=causal_iv_fit_2sls]{causal_iv_fit_2sls()}} with the PCs as instruments.
}
}
\details{
Using the top \code{n_pcs} principal components instead of raw
embedding dimensions keeps the instrument count manageable
(avoiding the curse of dimensionality) and ensures the instruments
are orthogonal (which simplifies the Sargan diagnostics).  The
default \code{n_pcs = 5L} yields a 4-over-identified model for a
single-exposure query, enabling the Sargan J-test.
}
