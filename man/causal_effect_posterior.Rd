% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_posterior.R
\name{causal_effect_posterior}
\alias{causal_effect_posterior}
\title{Posterior distribution of a backdoor-adjusted direct effect}
\usage{
causal_effect_posterior(
  data,
  dag,
  exposure,
  outcome,
  adjustment = NULL,
  estimator = c("lm", "bart"),
  cluster = "kmeans_cluster",
  B = 500L,
  seed = NULL,
  bart_kwargs = list(),
  delta = NULL,
  units = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{dag}{A \code{dagitty} DAG (required when \code{adjustment = NULL}).}

\item{exposure, outcome}{Character; column names.}

\item{adjustment}{Optional character vector of adjustment-set
columns; derived from the DAG if \code{NULL}.}

\item{estimator}{\code{"lm"} or \code{"bart"}.}

\item{cluster, B, seed}{See \code{\link[=causal_effect_bootstrap]{causal_effect_bootstrap()}}; only used
for the LM estimator.}

\item{bart_kwargs}{Named list of extra arguments forwarded to
\code{dbarts::bart()} when \code{estimator = "bart"}.}

\item{delta}{Numeric; counterfactual increment for the BART
estimator (defaults to \code{IQR(exposure) / 2}).}

\item{units}{Optional character; free-text tag passed through to
the \code{edaphos_posterior}.}
}
\value{
An \code{edaphos_posterior} with \code{query_type = "effect"} and
\code{method} set to \code{"bootstrap"} (LM) or \code{"bayesian"} (BART).
}
\description{
Unified v1.6.0 entry point for Pillar 1. Returns an
\code{\link[=edaphos_posterior]{edaphos_posterior()}} with posterior draws over the identified
direct effect, ready for \code{\link[=uncertainty_calibrate]{uncertainty_calibrate()}} and
\code{\link[ggplot2:autoplot]{ggplot2::autoplot()}}. For the LM estimator the draws are a
cluster-block bootstrap; for BART the draws are the native Markov-
chain posterior.
}
