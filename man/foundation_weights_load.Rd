% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/foundation_weights.R
\name{foundation_weights_load}
\alias{foundation_weights_load}
\title{Load a pretrained encoder into an \code{edaphos_foundation_moco} wrapper}
\usage{
foundation_weights_load(
  source,
  n_channels = NULL,
  feature_dim = NULL,
  proj_dim = NULL,
  cache_dir = NULL,
  overwrite = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{source}{Either a character name registered in
\code{\link[=foundation_weights_list]{foundation_weights_list()}} — in which case the encoder is
downloaded (cached) via \code{\link[=foundation_weights_download]{foundation_weights_download()}} — or a
path to a local \code{.pt} file. For the local path case, pass the
matching metadata as \code{n_channels} / \code{feature_dim} / etc.}

\item{n_channels, feature_dim, proj_dim}{Optional integers; when
\code{source} is a local path these must describe the shape the
\code{.pt} file was saved from. When \code{source} is a registry name they
are pulled from the registry.}

\item{cache_dir, overwrite, verbose}{Forwarded to
\code{\link[=foundation_weights_download]{foundation_weights_download()}} when \code{source} is a registry
name.}
}
\value{
An \code{edaphos_foundation_moco} object with a populated
\verb{$encoder_q} (the query encoder; the momentum encoder is not
restored because it is only needed during training).
}
\description{
Restores a pretrained MoCo v2 encoder from a \code{.pt} state-dict file
(or by name from the published registry) into an in-memory
\code{edaphos_foundation_moco} object that is \strong{shape-compatible} with
the output of \code{\link[=foundation_moco_pretrain_tiles]{foundation_moco_pretrain_tiles()}}. The restored
object is ready to feed into \code{\link[=foundation_moco_embed]{foundation_moco_embed()}},
\code{\link[=foundation_fit_classifier]{foundation_fit_classifier()}} or \code{\link[=foundation_fit_regressor]{foundation_fit_regressor()}}
without any additional plumbing.
}
\examples{
\dontrun{
  moco <- foundation_weights_load("edaphos-cerrado-moco-v1")
  emb  <- foundation_moco_embed(moco, patches)
}
}
