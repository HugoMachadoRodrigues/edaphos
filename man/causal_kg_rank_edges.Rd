% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_kg_io.R
\name{causal_kg_rank_edges}
\alias{causal_kg_rank_edges}
\title{Rank Knowledge-Graph edges by evidence strength}
\usage{
causal_kg_rank_edges(
  kg,
  by = c("n_sources", "mean_confidence", "agrovoc_support"),
  alignment = NULL,
  top_n = NULL
)
}
\arguments{
\item{kg}{An \code{edaphos_causal_kg}.}

\item{by}{Character vector of ranking metrics, any subset of
\code{c("n_sources", "mean_confidence", "agrovoc_support")}, in
priority order.}

\item{alignment}{Optional data frame as returned by
\code{\link[=causal_kg_alignment]{causal_kg_alignment()}} (must have columns \code{original},
\code{canonical}, plus \code{uri} when derived from
\code{vocab = "agrovoc"}). Used to compute \code{agrovoc_support}.}

\item{top_n}{Optional integer — return at most this many rows.
\code{NULL} returns the full ranking.}
}
\value{
A data frame with columns
\code{cause}, \code{effect}, \code{n_sources}, \code{mean_confidence},
\code{max_confidence}, \code{sources} (collapsed " | "-separated),
\code{evidence} (collapsed), and — when available —
\code{agrovoc_support}, \code{agrovoc_cause}, \code{agrovoc_effect}.
}
\description{
Collapses a \code{edaphos_causal_kg} to one row per unique
(cause, effect) edge and ranks the result by a user-selected
metric:
}
\details{
\describe{
\item{\code{"n_sources"}}{Number of distinct sources supporting the
edge (counted by splitting the \code{|}-separated \code{source} field
on the underlying \code{igraph} edge-attribute). The single most
informative signal for an LLM-extracted KG built over thousands
of papers: an edge asserted by 50 papers is far more
trustworthy than one asserted by 1.}
\item{\code{"mean_confidence"}}{Mean LLM confidence across extractions
that produced the same (cause, effect) pair.}
\item{\code{"agrovoc_support"}}{Fraction of the pair's endpoints
(cause, effect) that resolve to a FAO AGROVOC concept under an
\code{alignment} mapping (0, 0.5 or 1). Requires either
\code{alignment} to be supplied or \code{kg} already aligned; when
neither is available the column is \code{NA}.}
}

The return is a tidy data frame sorted in descending order by
\emph{all} ranking columns requested in \code{by} (i.e. the first \code{by}
element is the primary sort key, the second breaks ties, etc.).
This is usually more informative than a single-metric ranking —
an edge that has both many sources AND high confidence is more
trustworthy than either alone.
}
\examples{
\donttest{
  kg <- causal_kg_new()
  kg <- causal_kg_add_edge(kg, "precipitation", "soc",
                            source = "Jenny 1941", confidence = 0.9)
  kg <- causal_kg_add_edge(kg, "precipitation", "soc",
                            source = "Minasny 2017", confidence = 0.85)
  causal_kg_rank_edges(kg, by = c("n_sources", "mean_confidence"))
}
}
\seealso{
\code{\link[=causal_kg_alignment]{causal_kg_alignment()}}, \code{\link[=summary.edaphos_causal_kg]{summary.edaphos_causal_kg()}}.
}
