% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm_annotation.R
\name{llm_annotation_launch}
\alias{llm_annotation_launch}
\title{Launch the interactive gold-standard review app}
\usage{
llm_annotation_launch(
  draft_path,
  output_path = NULL,
  keyboard_shortcuts = TRUE,
  port = NULL
)
}
\arguments{
\item{draft_path}{Path to the draft JSONL produced by
\code{\link[=llm_preannotate]{llm_preannotate()}}.}

\item{output_path}{Where to write the reviewed JSONL.  Defaults to
\code{draft_path} (in-place review).}

\item{keyboard_shortcuts}{Logical; enable keyboard bindings.}

\item{port}{Optional integer port for the Shiny app.}
}
\value{
Called for its side-effect (launches app); invisibly
returns the path of the reviewed JSONL.
}
\description{
Opens a Shiny application that presents every abstract in the draft
JSONL and lets the annotator accept / edit / reject each LLM-drafted
claim, plus add any claim the LLM missed.  Writes to \code{output_path}
after every abstract, so interrupted sessions resume exactly where
they left off.
}
\details{
\strong{Keyboard shortcuts} (when \code{keyboard_shortcuts = TRUE}):
\code{a} — accept all and next · \code{r} — reject all · \code{n} — next abstract
(save) · \code{p} — previous · \code{+} — add claim · \verb{1..9} — toggle
accept on claim \emph{n}.
}
