% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_iv.R
\name{causal_iv_posterior}
\alias{causal_iv_posterior}
\title{Bootstrap posterior for a 2SLS effect as an edaphos_posterior}
\usage{
causal_iv_posterior(
  data,
  exposure,
  outcome,
  instruments,
  covariates = NULL,
  B = 500L,
  cluster = NULL,
  seed = NULL,
  units = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{exposure}{Character; name of the endogenous exposure column.}

\item{outcome}{Character; name of the outcome column.}

\item{instruments}{Character vector; names of instrument columns.
More instruments than exposures gives an over-identified model
on which the Sargan test is applicable.}

\item{covariates}{Optional character vector of exogenous-control
column names included in both first and second stage.}

\item{B}{Integer; number of bootstrap resamples.  Default \code{500L}.}

\item{cluster}{Optional character; column in \code{data} to resample by
block (e.g. \code{"kmeans_cluster"} for spatial resampling).}

\item{seed}{Optional RNG seed.}

\item{units}{Optional units string.}
}
\value{
An \code{edaphos_posterior} with \code{query_type = "effect"} and
\code{method = "bootstrap"}.
}
\description{
Wraps \code{\link[=causal_iv_fit_2sls]{causal_iv_fit_2sls()}} in a nonparametric bootstrap over
rows (or over clusters if \code{cluster} is supplied) and packages the
resulting vector of effect estimates as an \code{\link[=edaphos_posterior]{edaphos_posterior()}}
so that \code{\link[=uncertainty_calibrate]{uncertainty_calibrate()}} and \code{\link[ggplot2:autoplot]{ggplot2::autoplot()}} apply
uniformly.
}
