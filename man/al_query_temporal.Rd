% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal_active_learning.R
\name{al_query_temporal}
\alias{al_query_temporal}
\title{Temporal Active Learning: rank candidate cells by their Kalman
gain norm after the latest EnKF assimilation}
\usage{
al_query_temporal(
  kalman_update,
  candidate_coords = NULL,
  n_select = 10L,
  combine = c("gain", "gain_sd", "gain_sd_normalised")
)
}
\arguments{
\item{kalman_update}{A \code{edaphos_temporal_kalman} object returned
by \code{\link[=temporal_kalman_update]{temporal_kalman_update()}}.  Must carry the \code{gain_row_norm}
and \code{analysis_sd} fields (they are produced by default).}

\item{candidate_coords}{Optional data frame with \code{lon}, \code{lat}
columns that restrict scoring to a finite set of physically
accessible cells.  When \code{NULL}, every cell of the analysis grid
is a candidate.}

\item{n_select}{Integer; how many candidates to return.}

\item{combine}{One of \code{"gain"} (rank by pure gain norm, the v1.5.0
default), \code{"gain_sd"} (weighted product of gain and remaining
analysis SD), or \code{"gain_sd_normalised"} (same but each term is
percentile-normalised first).}
}
\value{
Data frame sorted by descending priority with columns
\code{row}, \code{col}, \code{gain}, \code{analysis_sd}, \code{priority}.
}
\description{
Closes the loop between Pillar 3 (4D ConvLSTM + stochastic EnKF
assimilation) and Pillar 5 (autonomous active learning).  The
Kalman-gain norm is a forward-looking estimate of how much the
posterior ensemble would shrink if a new observation were placed
at each spatial cell; candidates with high gain are the natural
next sampling locations.
}
