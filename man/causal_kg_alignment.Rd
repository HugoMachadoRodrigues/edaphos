% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_ontology.R
\name{causal_kg_alignment}
\alias{causal_kg_alignment}
\title{Align Knowledge-Graph node labels to a canonical vocabulary}
\usage{
causal_kg_alignment(
  kg,
  vocab = NULL,
  method = c("exact", "substring", "fuzzy"),
  max_distance = 4L,
  agrovoc_cache = NULL,
  agrovoc_batch = FALSE,
  agrovoc_max_active = 5L
)
}
\arguments{
\item{kg}{An \code{edaphos_causal_kg}.}

\item{vocab}{Either a character vector / data frame of canonical
terms, the string \code{"cerrado"} (default; uses
\code{\link[=causal_ontology_cerrado]{causal_ontology_cerrado()}}) or the string \code{"agrovoc"} which
triggers a \strong{live SPARQL query} to the FAO AGROVOC endpoint
via \code{\link[=causal_ontology_agrovoc_align]{causal_ontology_agrovoc_align()}}. For AGROVOC the
alignment type is reported as \code{"agrovoc"} instead of exact /
substring / fuzzy.}

\item{method}{Which matcher tier(s) to enable (ignored when
\code{vocab = "agrovoc"}). Any combination of \code{"exact"},
\code{"substring"}, \code{"fuzzy"}.}

\item{max_distance}{Fuzzy-matcher Levenshtein cap.}

\item{agrovoc_cache}{Optional \code{.rds} path used by
\code{vocab = "agrovoc"} to avoid re-querying the same terms.}

\item{agrovoc_batch}{Logical — when \code{TRUE} and \code{vocab = "agrovoc"},
uses the parallel-dispatch variant
\code{\link[=causal_ontology_agrovoc_align_batch]{causal_ontology_agrovoc_align_batch()}} to resolve all nodes in
flight. Recommended for KGs with more than ~20 unique nodes.}

\item{agrovoc_max_active}{Integer — concurrency for the parallel
variant. Only consulted when \code{agrovoc_batch = TRUE}.}
}
\value{
A data frame with columns \code{original}, \code{canonical},
\code{method}, \code{distance}. When \code{vocab = "agrovoc"} an extra
\code{uri} column is attached.
}
\description{
Computes the mapping from node labels currently present in an
\code{edaphos_causal_kg} onto their canonical counterparts in a target
vocabulary (\code{causal_ontology_cerrado()} by default). Three matchers
are tried in order — exact, substring, fuzzy — and the first hit
wins. The mapping is returned as a tidy data frame; apply it with
\code{\link[=causal_kg_rename]{causal_kg_rename()}}.
}
