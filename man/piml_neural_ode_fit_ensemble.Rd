% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/piml_neural_ode_ensemble.R
\name{piml_neural_ode_fit_ensemble}
\alias{piml_neural_ode_fit_ensemble}
\title{Train a deep ensemble of Neural ODEs for uncertainty quantification}
\usage{
piml_neural_ode_fit_ensemble(
  depths,
  values,
  y_surface = NULL,
  K = 5L,
  hidden = c(16L, 16L),
  n_steps = 4L,
  epochs = 500L,
  lr = 0.01,
  seed = NULL,
  verbose = FALSE
)
}
\arguments{
\item{depths, values, y_surface, hidden, n_steps, epochs, lr, verbose}{Forwarded to \code{\link[=piml_neural_ode_fit]{piml_neural_ode_fit()}} member-by-member. See there
for details.}

\item{K}{Integer — number of ensemble members. Default 5 for a
reasonable speed/variance trade-off; 10 is the recommended
ceiling on laptop-scale problems.}

\item{seed}{Optional integer — seeds the ensemble. Member \code{k} is
trained with \code{seed = seed + k - 1L}.}
}
\value{
An \code{edaphos_piml_neural_ode_ensemble} carrying:
\describe{
\item{members}{A list of \code{K} fitted \code{edaphos_piml_neural_ode}
objects.}
\item{K, hidden, n_steps, y_surface}{Configuration echo.}
\item{fitted}{A \verb{K × n_obs} matrix of in-sample predictions.}
\item{fitted_mean, fitted_sd}{Ensemble mean / standard deviation
of \code{fitted}.}
\item{rmse}{RMSE of the ensemble mean against the training
observations.}
}
}
\description{
Wraps \code{\link[=piml_neural_ode_fit]{piml_neural_ode_fit()}} in a K-member ensemble, each member
trained from an independent random initialisation. The returned
object behaves like a single \code{edaphos_piml_neural_ode} for the
purposes of \code{\link[=predict]{predict()}} — except that the method returns the full
\verb{K × length(newdepths)} matrix of member-wise predictions, or
(optionally) a tidy \verb{(mean, sd, lower, upper)} credible-interval
summary.
}
\details{
The theoretical justification for the ensemble as a posterior
approximation is developed in Lakshminarayanan et al. 2017 and
Wilson & Izmailov 2020 (see the @references section). In short:
for wide neural networks, different
SGD trajectories converge to different basins of the loss surface,
and the resulting member-wise spread is a well-calibrated proxy for
the Bayesian posterior predictive variance.
}
\examples{
\dontrun{
  depths <- c(5, 15, 30, 60, 100)
  values <- c(25, 18, 12, 8, 6.5)
  ens <- piml_neural_ode_fit_ensemble(depths, values, K = 5L,
                                        epochs = 300L, seed = 1L)
  predict(ens, newdepths = c(10, 20, 40, 80), interval = 0.95)
}
}
\references{
Lakshminarayanan, B., Pritzel, A. and Blundell, C. (2017). Simple
and scalable predictive uncertainty estimation using deep
ensembles. \emph{NeurIPS 30}, 6402–6413.

Wilson, A. G. and Izmailov, P. (2020). Bayesian deep learning and
a probabilistic perspective of generalization. \emph{NeurIPS 33},
4697–4708.
}
\seealso{
\code{\link[=predict.edaphos_piml_neural_ode_ensemble]{predict.edaphos_piml_neural_ode_ensemble()}},
\code{\link[=piml_neural_ode_fit]{piml_neural_ode_fit()}} for the single-member variant,
\code{\link[=piml_profile_fit_bayesian]{piml_profile_fit_bayesian()}} for the parametric-ODE analogue.
}
