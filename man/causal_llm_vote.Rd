% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_llm_vote.R
\name{causal_llm_vote}
\alias{causal_llm_vote}
\title{Multi-extractor consensus over LLM-extracted causal claims}
\usage{
causal_llm_vote(
  abstract,
  backends,
  voting = c("majority", "weighted", "intersection"),
  min_support = NULL,
  threshold = NULL,
  weights = NULL,
  timeout_sec = 120
)
}
\arguments{
\item{abstract}{Character scalar with the passage to annotate.}

\item{backends}{List of backend configurations; see above.}

\item{voting}{\code{"majority"} (default), \code{"weighted"}, or
\code{"intersection"}.}

\item{min_support}{Integer — required number of backends for
\code{"majority"} voting. Defaults to \code{ceiling(length(backends) / 2)}.}

\item{threshold}{Numeric — weighted-confidence threshold for
\code{"weighted"} voting. Defaults to \code{0.5 * sum(weights)}.}

\item{weights}{Optional numeric vector, one element per backend
(same order as \code{backends}). Defaults to uniform weights.}

\item{timeout_sec}{Default per-request timeout; may be overridden
per-backend via the \code{timeout_sec} field of each config entry.}
}
\value{
A data frame with one row per consensus edge and columns
\code{cause}, \code{effect}, \code{n_backends}, \code{mean_confidence},
\code{weighted_confidence}, \code{backends} (\code{" | "}-separated list of
supporting backend ids), \code{evidence} (concatenated quotations).
}
\description{
Runs \strong{N LLM backends} on the same abstract and returns a single
consensus data frame that resolves the inevitable disagreements by
one of three voting rules. See the file header for the design
rationale.
}
\section{Voting rules}{

\describe{
\item{\code{"majority"} (default)}{Keep edges asserted by at least
\code{min_support} backends. When \code{min_support = NULL}, defaults to
\code{ceiling(length(backends) / 2)} so 2-out-of-3 or 3-out-of-5
agreement survives.}
\item{\code{"weighted"}}{Keep edges whose weighted confidence
\eqn{\sum_i w_i c_i} exceeds \code{threshold}. Weights are
supplied per backend via \code{weights}.}
\item{\code{"intersection"}}{Keep only edges asserted by \strong{every}
backend. Most conservative; maximises precision at the
expense of recall.}
}
}

\section{Expected \code{backends} shape}{

A list of lists. Each inner list configures one backend and must
contain at least a \code{backend} field (one of \code{"ollama"},
\code{"openai"}, \code{"anthropic"}) and may optionally carry \code{model},
\code{host} (for Ollama), \code{temperature}, \code{timeout_sec}, \code{api_key},
\code{id}. Example:

\preformatted{
backends = list(
  list(backend = "ollama",    model = "gemma4:latest",
       host   = "http://localhost:11434"),
  list(backend = "openai",    model = "gpt-4o-mini"),
  list(backend = "anthropic", model = "claude-sonnet-4-6")
)
}

Failed backends (timeouts, missing API keys) emit a warning and
contribute zero claims; the vote continues with the remaining
backends.
}

\examples{
\dontrun{
  backends <- list(
    list(backend = "ollama",    model = "gemma4:latest"),
    list(backend = "openai",    model = "gpt-4o-mini"),
    list(backend = "anthropic", model = "claude-sonnet-4-6")
  )
  cons <- causal_llm_vote(
    abstract = "In Cerrado Oxisols, precipitation drives SOC...",
    backends = backends,
    voting   = "majority"
  )
  cons
}
}
\seealso{
\code{\link[=causal_llm_ingest_abstract_voted]{causal_llm_ingest_abstract_voted()}} for the KG-insertion
wrapper; \code{\link[=causal_llm_extract]{causal_llm_extract()}} for the single-backend call.
}
