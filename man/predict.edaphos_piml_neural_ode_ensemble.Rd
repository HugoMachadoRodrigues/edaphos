% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/piml_neural_ode_ensemble.R
\name{predict.edaphos_piml_neural_ode_ensemble}
\alias{predict.edaphos_piml_neural_ode_ensemble}
\title{Predictive posterior from a Neural-ODE deep ensemble}
\usage{
\method{predict}{edaphos_piml_neural_ode_ensemble}(
  object,
  newdepths,
  interval = NULL,
  include_obs_noise = FALSE,
  seed = NULL,
  ...
)
}
\arguments{
\item{object}{An \code{edaphos_piml_neural_ode_ensemble}.}

\item{newdepths}{Numeric vector of depths.}

\item{interval}{Optional numeric in \verb{(0, 1)} — when supplied,
returns a summary data frame with a symmetric central
credible interval at the requested level. When \code{NULL} (default)
returns the raw \verb{K × length(newdepths)} matrix.}

\item{include_obs_noise}{Logical — when \code{TRUE}, adds residual
Gaussian noise (with SD equal to the pooled member-wise
training RMSE) to every predictive draw so the interval
represents the predictive distribution of a \emph{future observation}
rather than the uncertainty on the \emph{mean function} alone.
Default \code{FALSE}.}

\item{seed}{Optional integer — RNG seed for the observation-noise
draw. Only consulted when \code{include_obs_noise = TRUE}.}

\item{...}{Unused.}
}
\value{
Either a numeric matrix (when \code{interval} is NULL) or a
data frame with columns \code{depth}, \code{mean}, \code{sd}, \code{lower}, \code{upper}.
}
\description{
Evaluates every ensemble member at the requested depths and returns
either the raw \verb{K × length(newdepths)} matrix of member-wise
predictions or a tidy \verb{(mean, sd, lower, upper)} summary
corresponding to a symmetric central credible interval computed
directly from the empirical ensemble distribution.
}
