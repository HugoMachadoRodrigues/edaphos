% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/piml_hierarchical.R
\name{piml_hierarchical_fit}
\alias{piml_hierarchical_fit}
\title{Hierarchical Neural ODE over multiple pedons (Pillar 2 × Pillar 5)}
\usage{
piml_hierarchical_fit(
  pedons,
  id_col,
  depth_col,
  value_col,
  covariate_cols,
  hidden = c(32L, 16L),
  y0_hidden = c(16L),
  n_steps = 4L,
  epochs = 500L,
  lr = 0.01,
  seed = NULL,
  verbose = FALSE
)
}
\arguments{
\item{pedons}{Data frame in long form (one row per horizon) with
columns for pedon id, depth, value, and one or more covariates.}

\item{id_col, depth_col, value_col}{Character, column names.}

\item{covariate_cols}{Character vector, names of covariate columns.
Assumed constant within a pedon — the first row of each pedon is
used.}

\item{hidden}{Integer vector, MLP widths for the ODE vector field
\eqn{f_\theta}.}

\item{y0_hidden}{Integer vector, MLP widths for the surface-value
head \eqn{y_0(\mathbf{x})}.}

\item{n_steps}{Integer, RK4 steps between successive observation
depths.}

\item{epochs, lr}{Integer and numeric — Adam hyperparameters.}

\item{seed, verbose}{As elsewhere in edaphos.}
}
\value{
A \code{edaphos_piml_hierarchical} object (S3).
}
\description{
Fits a covariate-conditioned Neural ODE
\eqn{dy/dz = f_\theta(z, y, \mathbf{x})}
jointly across every pedon in a long-format data frame, plus a
sibling MLP \eqn{y_0(\mathbf{x})} that predicts the surface value
from covariates. The two nets share the optimiser so training sees
all pedons at once.
}
\details{
Once fitted, \code{piml_hierarchical_predict()} returns a profile for \strong{any
new location} given its covariates — no horizon measurement required.
Pair with \code{\link[=al_physics_gate_piml_hierarchical]{al_physics_gate_piml_hierarchical()}} to use that per-location
envelope as a rejection filter inside the Active Learning loop.
}
