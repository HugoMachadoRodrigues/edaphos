% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_sensitivity.R
\name{causal_sensitivity_summary}
\alias{causal_sensitivity_summary}
\title{Cinelli & Hazlett (2020) sensitivity summary for a causal effect}
\usage{
causal_sensitivity_summary(effect, se, df, q = 1, alpha = 0.05)
}
\arguments{
\item{effect}{Numeric; point estimate of the causal effect.}

\item{se}{Numeric; standard error of the estimate.}

\item{df}{Numeric; degrees of freedom (n - k - 1).}

\item{q}{Numeric; the fraction of the estimate we want to neutralise.
\code{q = 1} is zero-out; \code{q = 0.5} is half-reduction.}

\item{alpha}{Significance level for RV_alpha (default \code{0.05}).}
}
\value{
Named list with \code{rv}, \code{rv_alpha}, \code{t_stat}, and a verbal
interpretation.
}
\description{
Computes the \strong{Robustness Value (RV)}: the minimum partial
R-squared (on \emph{both} exposure and outcome, with the observed
adjustments partialed out) that an unobserved confounder U would
need to have to bring the estimate to zero (\code{q = 1}) or reduce it
by a fraction \code{q} of its magnitude.  Also reports the \strong{RV at 5\%
significance} (RV_q): the minimum R-squared to push the t-ratio
below the critical value.
}
\details{
Interpretation: if RV = 0.10, then any confounder explaining more
than 10 percent of the residual variance in both X and Y (jointly)
would be enough to kill the effect.  Small RV = fragile estimate.
}
\examples{
# A 2SLS effect of beta = 0.008, SE = 0.003, df = 1080:
causal_sensitivity_summary(0.008, 0.003, 1080)
}
