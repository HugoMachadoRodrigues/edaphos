% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/piml_bayesian.R
\name{predict.edaphos_piml_bayes}
\alias{predict.edaphos_piml_bayes}
\title{Posterior predictive distribution of a Bayesian Pillar 2 fit}
\usage{
\method{predict}{edaphos_piml_bayes}(
  object,
  newdepths,
  n_draws = NULL,
  interval = NULL,
  include_obs_noise = FALSE,
  seed = NULL,
  ...
)
}
\arguments{
\item{object}{An \code{edaphos_piml_bayes} returned by
\code{\link[=piml_profile_fit_bayesian]{piml_profile_fit_bayesian()}}.}

\item{newdepths}{Numeric vector of depths at which to evaluate the
predictive posterior.}

\item{n_draws}{Integer — number of posterior draws to propagate.
Defaults to \code{min(500, nrow(object$draws))}.}

\item{interval}{Optional numeric scalar in \verb{(0, 1)}. When given,
returns a tidy \code{data.frame} with \code{mean}, \code{sd}, \code{lower}, \code{upper}
per depth (symmetric central credible interval). When \code{NULL}
(default), returns the full \code{n_draws}-by-length(newdepths) matrix
of predictive draws.}

\item{include_obs_noise}{Logical — if \code{TRUE}, Gaussian observation
noise \code{N(0, sigma^2)} is added to every predictive draw so the
interval represents the predictive distribution of a \emph{future
observation}. If \code{FALSE} (default), the interval represents the
uncertainty on the \emph{mean function} \eqn{g(z; \theta)} alone.}

\item{seed}{Optional integer seed for the sub-sampling of draws.}

\item{...}{Unused; present for S3 \code{predict} generic compatibility.}
}
\value{
Either a matrix (when \code{interval} is NULL) or a data frame
with columns \code{depth}, \code{mean}, \code{sd}, \code{lower}, \code{upper}.
}
\description{
Propagates the full posterior over ODE parameters through the
forward model to produce posterior-predictive draws of
\eqn{y(z)} at user-supplied depths.
}
\examples{
depths <- c(5, 15, 30, 60, 100)
values <- c(25, 18, 12, 8, 6.5)
fit <- piml_profile_fit_bayesian(depths, values)
predict(fit, newdepths = c(10, 20, 40, 80), interval = 0.95)
}
