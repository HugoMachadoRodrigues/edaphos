% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantum_kernel.R
\name{quantum_krr_fit}
\alias{quantum_krr_fit}
\title{Fit a Quantum Kernel Ridge Regression (Pillar 6)}
\usage{
quantum_krr_fit(X, y, reps = 2L, lambda = 0.1)
}
\arguments{
\item{X}{Feature matrix already rescaled to \verb{[0, pi]} —
\code{\link[=quantum_scale]{quantum_scale()}} is the recommended preprocessor.}

\item{y}{Numeric response vector (or \verb{\\pm 1} for classification).}

\item{reps}{Integer, encoding depth of the ZZFeatureMap.}

\item{lambda}{Numeric ridge regulariser (\verb{> 0}).}
}
\value{
An \code{edaphos_quantum_krr} object.
}
\description{
Closed-form dual solution
\deqn{\boldsymbol{\alpha} = (K + \lambda I)^{-1}\mathbf{y}}
using the quantum Gram matrix from \code{\link[=quantum_kernel]{quantum_kernel()}}. Works both
for regression (\code{y} numeric) and binary classification (encode
\verb{y \\in \\\{-1, +1\\\}} and threshold the prediction at zero).
}
\examples{
\donttest{
  set.seed(1)
  X <- quantum_scale(matrix(runif(60), ncol = 3L))
  y <- sign(X[, 1L] - mean(X[, 1L]))
  fit <- quantum_krr_fit(X, y, reps = 2L, lambda = 0.1)
  mean(predict(fit, X, type = "class") == y)  # training accuracy
}
}
