% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/foundation_simclr.R
\name{foundation_simclr_pretrain}
\alias{foundation_simclr_pretrain}
\title{SimCLR pre-training on raster covariate patches (Pillar 4 scaffold)}
\usage{
foundation_simclr_pretrain(
  patches,
  feature_dim = 32L,
  proj_dim = 16L,
  batch_size = 8L,
  epochs = 30L,
  lr = 0.005,
  temperature = 0.2,
  noise_sd = 0.1,
  seed = NULL,
  verbose = FALSE
)
}
\arguments{
\item{patches}{A 4-D R array shaped \verb{(N, C, H, W)}: \code{N} patches, \code{C}
covariate channels, spatial \verb{H x W}.}

\item{feature_dim, proj_dim}{Integer; backbone and projection head
widths.}

\item{batch_size}{Integer; SimCLR mini-batch size. Each batch
contributes \code{2 * batch_size - 2} negatives per anchor.}

\item{epochs, lr}{Training hyperparameters for Adam.}

\item{temperature}{Numeric; NT-Xent temperature.}

\item{noise_sd}{Numeric; additive-noise strength during augmentation.}

\item{seed, verbose}{As elsewhere.}
}
\value{
A \code{edaphos_foundation_simclr} object (S3).
}
\description{
Trains a small CNN encoder on unlabeled raster patches via the SimCLR
contrastive objective. Each forward pass draws two independent
augmented views of every patch in the mini-batch and enforces high
embedding similarity between views of the same patch and low
similarity otherwise.
}
