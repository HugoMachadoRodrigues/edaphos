% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark_wosis.R
\name{benchmark_fit_p10_gat}
\alias{benchmark_fit_p10_gat}
\title{Benchmark wrapper: Pilar 10 -- GAT seed-ensemble on k-NN graph}
\usage{
benchmark_fit_p10_gat(
  train,
  test,
  cov_cols,
  k = 8L,
  hidden = 12L,
  n_heads = 2L,
  n_layers = 2L,
  epochs = 100L,
  lr = 0.03,
  n_ensemble = 10L,
  seed = 1L,
  calibrate = TRUE
)
}
\arguments{
\item{train, test}{Data frames with \code{soc}, \code{lon}, \code{lat}, and
\code{cov_cols}.}

\item{cov_cols}{Character vector of covariate columns used as node
features.}

\item{k}{Integer; k-NN degree of the co-location graph.}

\item{hidden, n_heads, n_layers}{Architecture.  See \code{\link[=gnn_fit]{gnn_fit()}}.}

\item{epochs, lr}{Training hyperparameters.}

\item{n_ensemble}{Integer; number of seed-distinct fits.}

\item{seed}{Base RNG seed (each member uses \code{seed + b}).}

\item{calibrate}{Logical (default \code{TRUE}).  When \code{TRUE}, residual
noise from a representative full-data GAT fit is injected on
every posterior sample (see Pilar-1 wrapper for the rationale).}
}
\value{
An \code{edaphos_posterior} with
\code{method = "ensemble"}, \code{query_type = "map"}.
}
\description{
Builds a joint k-NN co-location graph on \code{rbind(train, test)}
using (lon, lat) for adjacency and \code{cov_cols} as node features,
then fits \code{n_ensemble} independent GAT regressors with different
seeds, and harvests predictions at the test nodes.  Predictive
posterior is the seed ensemble.
}
