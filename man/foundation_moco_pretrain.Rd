% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/foundation_moco.R
\name{foundation_moco_pretrain}
\alias{foundation_moco_pretrain}
\title{Pillar 4 -- MoCo v2 pre-training on raster covariate patches}
\usage{
foundation_moco_pretrain(
  patches,
  feature_dim = 64L,
  proj_dim = 32L,
  queue_size = 1024L,
  momentum = 0.999,
  temperature = 0.07,
  batch_size = 16L,
  epochs = 30L,
  lr = 0.03,
  crop_ratio = c(0.6, 1),
  flip_prob = 0.5,
  rot90_prob = 0.75,
  channel_drop_prob = 0.2,
  cutout_prob = 0.3,
  cutout_size_ratio = 0.2,
  brightness_jitter = 0.2,
  noise_sd = 0.1,
  seed = NULL,
  verbose = FALSE
)
}
\arguments{
\item{patches}{A 4-D array shaped \verb{(N, C, H, W)} -- \code{N} patches,
\code{C} covariate channels, spatial \verb{H x W}.}

\item{feature_dim, proj_dim}{Integer widths of the backbone output
and the contrastive projection head.}

\item{queue_size}{Integer \code{K} -- number of negatives stored in the
FIFO dictionary queue.}

\item{momentum}{Numeric in \verb{(0, 1)} -- EMA coefficient for the key
encoder (MoCo paper default is 0.999).}

\item{temperature}{Numeric \verb{> 0} -- InfoNCE temperature (MoCo v2
default is 0.07).}

\item{batch_size, epochs, lr}{Integer / numeric -- Adam optimiser
hyperparameters.}

\item{crop_ratio}{Numeric length-2 vector -- random-resized-crop
ratio range.}

\item{flip_prob, rot90_prob}{Probabilities of horizontal / vertical
flip and 90-deg rotation.}

\item{channel_drop_prob}{Probability of zeroing any given channel
independently.}

\item{cutout_prob, cutout_size_ratio}{Probability of spatial cutout
and its size ratio.}

\item{brightness_jitter}{Numeric \verb{[0, 1)} -- per-channel
multiplicative brightness range.}

\item{noise_sd}{Numeric \verb{>= 0} -- additive-noise standard deviation.}

\item{seed, verbose}{As elsewhere in the package.}
}
\value{
An \code{edaphos_foundation_moco} S3 object containing the
fitted query encoder (use \code{\link[=foundation_moco_embed]{foundation_moco_embed()}} to extract
embeddings), the key encoder, the training loss history and the
configuration.
}
\description{
Self-supervised momentum-contrastive pre-training (He et al. 2020;
Chen et al. 2020) with a raster-specific augmentation stack. Compared
to the SimCLR scaffold in \code{\link[=foundation_simclr_pretrain]{foundation_simclr_pretrain()}}, MoCo v2
introduces three architectural upgrades:
}
\details{
\itemize{
\item A \strong{momentum key encoder} updated by exponential moving
average: \eqn{\theta_k \leftarrow m\,\theta_k + (1-m)\,\theta_q}.
\item A \strong{dictionary queue} of past keys, so every mini-batch
sees \code{K} negatives rather than \verb{2B - 2}.
\item A wider residual CNN backbone (~feature_dim = 64 by default)
followed by a 2-layer projection head with BatchNorm, matching
the MoCo v2 recipe.
}

The augmentation stack is tuned for multi-channel raster patches
rather than natural photographs: spatial random resized crop,
horizontal / vertical flip, 90-degree rotations, per-channel
Bernoulli dropout (missing-band simulation), spatial cutout
(cloud-mask simulation), per-channel multiplicative brightness
jitter, and additive Gaussian sensor noise.
}
