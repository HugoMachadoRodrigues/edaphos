% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pillars_roadmap.R
\name{edaphos-roadmap}
\alias{edaphos-roadmap}
\title{Roadmap and status of the six pillars of edaphos}
\description{
Documentation-only landing page that summarises the scope of every
pillar, its current implementation status, and the governing
mathematical object. Use this together with the package overview —
\link{edaphos-package} — as the starting point for navigating the API.
}
\details{
\tabular{llll}{
   Pillar \tab Namespace \tab Status \tab Governing object \cr
   1. Causal AI \tab \verb{causal_*} \tab implemented \tab Structural causal model \eqn{G = (V, E)} with backdoor-adjusted estimand \eqn{\beta_{x \to y}^{\text{do}}} (Pearl, 2009); LM and BART estimators; LLM-driven Knowledge-Graph pipeline (\code{\link[=causal_kg_new]{causal_kg_new()}}, \code{\link[=causal_llm_extract]{causal_llm_extract()}}, \code{\link[=causal_augment_dag]{causal_augment_dag()}}) supporting Ollama / OpenAI / Anthropic; \strong{multi-extractor voting} (\code{\link[=causal_llm_vote]{causal_llm_vote()}}, \code{\link[=causal_llm_ingest_abstract_voted]{causal_llm_ingest_abstract_voted()}}) with majority / weighted / intersection rules for consensus across backends; paginated corpus clients for SciELO / OpenAlex + \code{\link[=causal_corpus_deduplicate]{causal_corpus_deduplicate()}}; resumable disk-cached ingestion (\code{\link[=causal_llm_ingest_corpus]{causal_llm_ingest_corpus()}} with \code{cache_dir} + \code{max_retries}); ontology alignment against a curated Cerrado vocabulary \strong{and live FAO AGROVOC SPARQL}, including \strong{concurrent batched alignment} (\code{\link[=causal_ontology_agrovoc_align_batch]{causal_ontology_agrovoc_align_batch()}}); \strong{bottom-up structure learning} from horizon data via \code{\link[=causal_structure_learn]{causal_structure_learn()}} (hc / tabu / pc-stable / mmhc backed by \code{bnlearn}) with bootstrap edge confidence; \strong{paper-scale persistence and audit} via \code{\link[=causal_kg_save]{causal_kg_save()}} / \code{\link[=causal_kg_load]{causal_kg_load()}} (portable RDS edge-list), \code{\link[=causal_kg_to_turtle]{causal_kg_to_turtle()}} (W3C RDF 1.1 Turtle export with reified provenance), \code{\link[=causal_kg_rank_edges]{causal_kg_rank_edges()}} (multi-source ranking) and a \code{summary()} method. \cr
   2. Physics-Informed ML \tab \verb{piml_*} \tab implemented \tab Pedogenetic ODE \eqn{dy/dz = -\lambda_0 e^{-\mu z}(y - y_\infty)} and Neural ODE \eqn{dy/dz = f_\theta(z, y, \mathbf{x})}; \strong{Bayesian posterior} over \eqn{(\lambda_0, \mu, y_\infty, y_0)} via Laplace approximation and adaptive random-walk Metropolis (\code{\link[=piml_profile_fit_bayesian]{piml_profile_fit_bayesian()}}); \strong{Neural-ODE deep ensemble} (\code{\link[=piml_neural_ode_fit_ensemble]{piml_neural_ode_fit_ensemble()}}) approximating the predictive posterior through K independent trajectories. \cr
   3. 4D Pedometry \tab \verb{temporal_*} \tab implemented \tab Stacked ConvLSTM (Shi et al., 2015) with seq-to-seq training, multi-step rollout and a mass-balance physics loss. \cr
   4. Foundation Models \tab \verb{foundation_*} \tab implemented \tab SimCLR scaffold (\code{\link[=foundation_simclr_pretrain]{foundation_simclr_pretrain()}}), MoCo v2 (\code{\link[=foundation_moco_pretrain]{foundation_moco_pretrain()}}) with momentum encoder + dictionary queue + raster-specific augmentations, and a \strong{planetary-scale tile pipeline} (\code{\link[=foundation_tile_source_soilgrids]{foundation_tile_source_soilgrids()}}, \code{\link[=foundation_tile_align]{foundation_tile_align()}}, \code{\link[=foundation_tile_dataset]{foundation_tile_dataset()}}, \code{\link[=foundation_moco_pretrain_tiles]{foundation_moco_pretrain_tiles()}} with \strong{Apple MPS / NVIDIA CUDA device dispatch}, \code{\link[=foundation_moco_embed_raster]{foundation_moco_embed_raster()}}); \strong{downstream fine-tuning API} (\code{\link[=foundation_fit_classifier]{foundation_fit_classifier()}}, \code{\link[=foundation_fit_regressor]{foundation_fit_regressor()}}) with linear-probe and full fine-tuning regimes + a two-group learning-rate schedule; \strong{public pretrained weights} distribution via Zenodo (\code{\link[=foundation_weights_list]{foundation_weights_list()}}, \code{\link[=foundation_weights_download]{foundation_weights_download()}}, \code{\link[=foundation_weights_load]{foundation_weights_load()}}) with SHA-256 verification and an on-disk cache under \code{tools::R_user_dir("edaphos")}. The first published encoder, \code{edaphos-cerrado-moco-v1}, was pretrained on 50 000 16x16 Cerrado tiles (SoilGrids + WorldClim + SRTM). \cr
   5. Autonomous Active Learning \tab \verb{al_*} \tab implemented \tab Hybrid policy \eqn{\pi(\mathbf{x}) = \alpha\,\tilde u(\mathbf{x}) + (1-\alpha)\,\tilde d(\mathbf{x})} with PIML-backed gate; \strong{BatchBALD information-theoretic batch acquisition} (\code{\link[=al_query_batchbald]{al_query_batchbald()}}) with greedy log-det submodular optimisation over QRF tree-level predictions. \cr
   6. Quantum ML \tab \verb{quantum_*} \tab implemented \tab Pure-R ZZFeatureMap (\code{\link[=quantum_feature_map]{quantum_feature_map()}}) + quantum-kernel Gram matrix (\code{\link[=quantum_kernel]{quantum_kernel()}}) + kernel ridge regression (\code{\link[=quantum_krr_fit]{quantum_krr_fit()}}); Qiskit-backed VQE bridge (\code{\link[=quantum_hamiltonian]{quantum_hamiltonian()}}, \code{\link[=quantum_hamiltonian_h2]{quantum_hamiltonian_h2()}}, \code{\link[=quantum_hamiltonian_organo_mineral]{quantum_hamiltonian_organo_mineral()}}, \code{\link[=quantum_vqe_fit]{quantum_vqe_fit()}}, \code{\link[=quantum_vqe_exact]{quantum_vqe_exact()}}) with three back ends — exact statevector, \strong{shot-based Aer with SPSA}, and \strong{full IBM Quantum Runtime dispatch} (\code{\link[=quantum_ibmq_submit]{quantum_ibmq_submit()}}, \code{\link[=quantum_ibmq_available]{quantum_ibmq_available()}}, \code{\link[=quantum_ibmq_backends]{quantum_ibmq_backends()}}, \code{\link[=quantum_ibmq_least_busy]{quantum_ibmq_least_busy()}}) with M3 readout mitigation and ZNE gate-folding error mitigation; \strong{qiskit-nature bridge} (\code{\link[=quantum_hamiltonian_from_pyscf]{quantum_hamiltonian_from_pyscf()}}, \code{\link[=quantum_hamiltonian_organo_mineral_nature]{quantum_hamiltonian_organo_mineral_nature()}}, \code{\link[=quantum_nature_total_energy]{quantum_nature_total_energy()}}) that lifts a user-supplied XYZ geometry through PySCF RHF + FreezeCore + ActiveSpace + ParityMapper into a qubit Hamiltonian, with curated organo-mineral presets (formic-acid carboxylate, methanediol ortho-diol, Fe(III)–formate mineral binding). \cr
}
}
\keyword{internal}
