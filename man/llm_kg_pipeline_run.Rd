% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm_kg_pipeline.R
\name{llm_kg_pipeline_run}
\alias{llm_kg_pipeline_run}
\title{Run the Pilar 1 LLM-KG pipeline on a (potentially large) corpus}
\usage{
llm_kg_pipeline_run(
  corpus_path,
  output_path,
  backend = c("ollama", "openai", "anthropic"),
  model = NULL,
  host = "http://localhost:11434",
  temperature = 0,
  timeout_sec = 120,
  max_retries = 3L,
  min_confidence = 0.5,
  verbose = TRUE,
  max_abstracts = NULL
)
}
\arguments{
\item{corpus_path}{Character path to a JSONL file with one
\verb{\{"source": ..., "abstract": ...\}} object per line.}

\item{output_path}{Character path to the JSONL claims output.
Created on first run; appended on resume.}

\item{backend}{One of \code{"ollama"}, \code{"openai"}, \code{"anthropic"}.}

\item{model}{LLM model identifier; defaults to backend-appropriate.}

\item{host}{Ollama HTTP host (ignored for hosted backends).}

\item{temperature}{Numeric; LLM sampling temperature.  Default \code{0}.}

\item{timeout_sec}{Numeric; per-call HTTP timeout.  Default \code{120}.}

\item{max_retries}{Integer; transient-error retry budget.
Default \code{3L}.}

\item{min_confidence}{Numeric; claims below this confidence are
discarded.  Default \code{0.5}.}

\item{verbose}{Logical; emit progress messages.  Default \code{TRUE}.}

\item{max_abstracts}{Optional integer; for testing, cap the run.}
}
\value{
Invisibly, a list with \code{n_processed}, \code{n_skipped},
\code{n_errors}, and the final \code{kg} object.
}
\description{
Drives \code{causal_llm_extract()} over a JSONL corpus of abstracts
(one JSON object with \code{source} + \code{abstract} per line) with
resumable JSONL checkpointing and throttle / retry.  The function
is engineered for 10 000+ abstract runs that can take several
hours to a day on a local Ollama instance.
}
\section{Resumability}{


On every successful per-abstract extraction, claims are appended
to \code{output_path} and the source identifier is appended to
\code{output_path.done}.  When \code{llm_kg_pipeline_run()} is restarted
on the same \code{output_path}, abstracts whose source is already in
the \code{.done} file are skipped automatically.  This makes the
pipeline safe under arbitrary process kills, network drops, or
Ollama restarts.
}

\section{Throttle / retry}{


Transient errors (HTTP 5xx, timeouts, JSON parse failures) are
retried up to \code{max_retries} times with exponential back-off
(1s, 2s, 4s, ...).  Persistent errors are logged to
\code{output_path.errors} and skipped without halting the run.
}

