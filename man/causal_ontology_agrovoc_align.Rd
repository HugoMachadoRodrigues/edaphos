% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_ontology.R
\name{causal_ontology_agrovoc_align}
\alias{causal_ontology_agrovoc_align}
\title{Live AGROVOC alignment for a vector of free-text terms}
\usage{
causal_ontology_agrovoc_align(
  terms,
  cache_path = NULL,
  max_per_term = 5L,
  endpoint = "https://agrovoc.fao.org/sparql",
  timeout_sec = 60L,
  verbose = FALSE
)
}
\arguments{
\item{terms}{Character vector of free-text terms (typically the
nodes of a Knowledge Graph).}

\item{cache_path}{Optional \code{.rds} file path. When supplied, the
function reads cached matches on entry and writes updated
matches on exit.}

\item{max_per_term}{Integer — how many candidates to request per
term (AGROVOC query LIMIT). The best one is kept.}

\item{endpoint}{Override the SPARQL endpoint URL.}

\item{timeout_sec}{Per-query timeout.}

\item{verbose}{Logical — print one line per queried term.}
}
\value{
A data frame with columns \code{term} (input), \code{uri}
(AGROVOC concept), \code{label} (AGROVOC pref label), \code{distance}
(Levenshtein of lowercased label vs term).
}
\description{
For each input \code{term}, queries the AGROVOC SPARQL endpoint for
concepts whose \code{skos:prefLabel} or \code{skos:altLabel} contains the
term, ranks the matches by Levenshtein distance to the canonical
label, and returns the best hit. Results are cached on disk via
\code{cache_path} so that repeated runs over the same vocabulary do
not re-query FAO.
}
