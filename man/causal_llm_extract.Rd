% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_llm.R
\name{causal_llm_extract}
\alias{causal_llm_extract}
\title{Extract causal claims from text via an LLM backend}
\usage{
causal_llm_extract(
  text,
  backend = c("ollama", "openai", "anthropic"),
  model = NULL,
  host = "http://localhost:11434",
  temperature = 0,
  timeout_sec = 120,
  api_key = NULL
)
}
\arguments{
\item{text}{Character scalar — the passage to annotate.}

\item{backend}{One of \code{"ollama"}, \code{"openai"}, \code{"anthropic"}.}

\item{model}{Optional model name; defaults vary by backend.}

\item{host}{Ollama server URL (ignored by the hosted backends).}

\item{temperature}{Sampling temperature. Defaults to \code{0} for
reproducibility.}

\item{timeout_sec}{Request timeout in seconds.}

\item{api_key}{Optional API key overriding the environment variable.}
}
\value{
A data frame with columns \code{cause}, \code{effect}, \code{evidence},
\code{confidence}. Empty (zero rows) when the model returns no claims
or the response cannot be parsed.
}
\description{
Calls a Large Language Model and returns a tidy table of causal
claims — one row per extracted \verb{(cause, effect)} pair — with the
quoted evidence and the confidence reported by the model.
}
\details{
Three backends are supported via a uniform prompt:
\itemize{
\item \code{"ollama"}  — local, zero-cost Ollama server (default
\code{host = "http://localhost:11434"}, default model
\code{"gemma4:latest"}). For higher extraction quality switch to
\code{model = "gemma4:26b"}.
\item \code{"openai"} — OpenAI Chat Completions API with JSON mode; needs
the \code{OPENAI_API_KEY} environment variable (default model
\code{"gpt-4o-mini"}).
\item \code{"anthropic"} — Claude Messages API; needs \code{ANTHROPIC_API_KEY}
(default model \code{"claude-sonnet-4-5"}).
}

The return value is backend-independent, so downstream
\code{\link[=causal_llm_ingest_abstract]{causal_llm_ingest_abstract()}} and \code{\link[=causal_kg_add_edge]{causal_kg_add_edge()}} work the
same regardless of which model produced the claims.
}
\examples{
\dontrun{
  abstract <- "In Cerrado soils, higher mean annual precipitation is
    associated with significantly increased topsoil organic carbon."
  causal_llm_extract(abstract, backend = "ollama",
                     model = "gemma4:latest")
}
}
