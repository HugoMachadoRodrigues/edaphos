% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal_cube.R
\name{temporal_synth_soc_cube}
\alias{temporal_synth_soc_cube}
\title{Generate a synthetic 4D soil-dynamics cube (Pillar 3 helper)}
\usage{
temporal_synth_soc_cube(
  H = 16L,
  W = 16L,
  T_total = 18L,
  seed = 1L,
  k_in = 0.03,
  k_out = 0.015,
  noise = 0.2
)
}
\arguments{
\item{H, W}{Integer, spatial grid size.}

\item{T_total}{Integer, total number of months.}

\item{seed}{Integer, RNG seed for reproducibility.}

\item{k_in, k_out}{Numeric rate coefficients (defaults are tuned so
SOC stays in a physically plausible 10-50 g/kg range).}

\item{noise}{Numeric, standard deviation of Gaussian process noise
on the SOC evolution (per pixel, per month).}
}
\value{
A list with elements
\describe{
\item{elev}{Numeric matrix \verb{H x W}.}
\item{precip}{Numeric array \verb{T x H x W}.}
\item{soc}{Numeric array \verb{T x H x W}.}
}
}
\description{
Builds a small spatio-temporal cube with realistic-looking dynamics
of Soil Organic Carbon (SOC) driven by monthly precipitation over a
static elevation field. The governing update is a minimalist
mass-balance
}
\details{
\deqn{
  \mathrm{SOC}_{t+1} = \mathrm{SOC}_t +
    k_{\text{in}}\, P_t -
    k_{\text{out}}\,\mathrm{SOC}_t\, P_t / \bar{P} +
    \varepsilon
}

with \verb{k_in, k_out} small, \eqn{P_t} the monthly precipitation, and
\eqn{\bar P} the long-term mean. Precipitation itself is a sinusoidal
seasonal cycle modulated by a west-east gradient so the cube shows
both temporal memory and spatial heterogeneity.

Use the resulting object to train \code{\link[=temporal_convlstm_fit]{temporal_convlstm_fit()}} and
forecast forward with \code{\link[=temporal_convlstm_rollout]{temporal_convlstm_rollout()}} in the Pillar 3
vignette.
}
\examples{
cube <- temporal_synth_soc_cube(H = 8, W = 8, T_total = 12, seed = 1)
dim(cube$soc)  # 12 x 8 x 8
}
