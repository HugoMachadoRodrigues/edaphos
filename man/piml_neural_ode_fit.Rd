% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/piml_neural_ode.R
\name{piml_neural_ode_fit}
\alias{piml_neural_ode_fit}
\title{Fit a Neural ODE depth profile (Pillar 2, deep variant)}
\usage{
piml_neural_ode_fit(
  depths,
  values,
  y_surface = NULL,
  hidden = c(16L, 16L),
  n_steps = 4L,
  epochs = 500L,
  lr = 0.01,
  seed = NULL,
  verbose = FALSE
)
}
\arguments{
\item{depths}{Numeric vector of horizon mid-depths.}

\item{values}{Numeric vector of observed values, same length.}

\item{y_surface}{Optional numeric; if supplied, $y_0$ is fixed (no
learnable parameter) — used when you trust a specific 0 cm reading.}

\item{hidden}{Integer vector with MLP hidden-layer widths.}

\item{n_steps}{Integer, RK4 steps between successive observation
depths. Higher = more accurate, slower.}

\item{epochs, lr}{Integer and numeric — training hyperparameters for
Adam.}

\item{seed}{Optional integer — forwarded to \code{torch::torch_manual_seed}
for reproducibility.}

\item{verbose}{Logical; print training loss every 50 epochs.}
}
\value{
A \code{edaphos_piml_neural_ode} object (S3).
}
\description{
Models the vector field \eqn{dy/dz = f_\theta(z, y)} where
\eqn{f_\theta} is a small MLP (default \verb{2 x 16} + tanh). The forward
model is a fixed-step RK4 integrator that runs end-to-end on \code{torch},
so training drives the MLP weights — and, optionally, the surface
value \eqn{y_0} — by back-propagating through the whole integration
(a proper Neural ODE, Chen et al. 2018).
}
\details{
Compared to \code{\link[=piml_profile_fit]{piml_profile_fit()}}, the Neural ODE variant can capture
\strong{non-monotone} profiles (E horizons below an A, bulge in a Bt) that
the parametric exponential-asymptote ODE cannot represent.
}
