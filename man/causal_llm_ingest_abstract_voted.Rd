% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal_llm_vote.R
\name{causal_llm_ingest_abstract_voted}
\alias{causal_llm_ingest_abstract_voted}
\title{Ingest an abstract into a KG via multi-extractor voting}
\usage{
causal_llm_ingest_abstract_voted(
  kg,
  abstract,
  source,
  backends,
  voting = "majority",
  min_support = NULL,
  threshold = NULL,
  weights = NULL,
  min_confidence = 0.5,
  timeout_sec = 120
)
}
\arguments{
\item{kg, abstract, source}{See \code{\link[=causal_llm_ingest_abstract]{causal_llm_ingest_abstract()}}.}

\item{backends, voting, min_support, threshold, weights, timeout_sec}{Forwarded to \code{\link[=causal_llm_vote]{causal_llm_vote()}}.}

\item{min_confidence}{Claims whose \code{mean_confidence} is below this
threshold are dropped before insertion.}
}
\value{
The updated \code{edaphos_causal_kg}. A \code{claims} attribute
carries the tidy consensus data frame that was actually
inserted, and a \code{per_backend} attribute carries the raw
per-backend claims for audit / debugging.
}
\description{
Single-call equivalent of running \code{\link[=causal_llm_vote]{causal_llm_vote()}} and then
\code{\link[=causal_kg_add_edge]{causal_kg_add_edge()}} on every surviving edge. The resulting KG
has a \code{source} tag of the form
\code{"<abstract_source> | vote(<voting>, n=<N_backends>)"} so the
provenance of every edge records both the underlying abstract and
the backends that agreed on the claim.
}
\examples{
\dontrun{
  backends <- list(
    list(backend = "ollama",    model = "gemma4:latest"),
    list(backend = "openai",    model = "gpt-4o-mini")
  )
  kg <- causal_kg_new()
  kg <- causal_llm_ingest_abstract_voted(
    kg,
    abstract = "Higher MAP drives SOC accumulation in Cerrado...",
    source   = "Ferreira 2021",
    backends = backends,
    voting   = "majority"
  )
}
}
