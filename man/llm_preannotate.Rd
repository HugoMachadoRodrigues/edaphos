% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm_annotation.R
\name{llm_preannotate}
\alias{llm_preannotate}
\title{Pre-annotate a corpus with an LLM to produce draft claims}
\usage{
llm_preannotate(
  corpus,
  backend = c("ollama", "openai", "anthropic", "simulator"),
  model = NULL,
  output_path = "cerrado_draft_gold.jsonl",
  cache_dir = NULL,
  max_abstracts = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{corpus}{Either a path to a JSONL file with one record per line
(records must have \code{abstract_id} and \code{abstract_text}), or a list
of records already in memory.}

\item{backend}{One of \code{"ollama"}, \code{"openai"}, \code{"anthropic"},
\code{"simulator"}.  When \code{"simulator"} the function uses
\code{\link[=llm_benchmark_simulate]{llm_benchmark_simulate()}} against a pseudo-gold-standard
derived from the abstract's first sentence -- useful for demos
and CI builds without API access.  Defaults to \code{"ollama"} with
\code{gemma4:latest}.}

\item{model}{Optional model id.  Defaults to \code{"gemma4:latest"} for
Ollama.}

\item{output_path}{Path to write the draft JSONL.}

\item{cache_dir}{Optional directory for per-record JSON caches.
When set, re-running the function over the same corpus
short-circuits to cached extractions, so interrupted jobs resume
exactly where they left off.  Defaults to \code{NULL} (no cache).}

\item{max_abstracts}{Optional integer cap on how many abstracts to
process (useful for staged runs).}

\item{verbose}{Logical; print per-abstract progress.}

\item{...}{Forwarded to \code{\link[=causal_llm_extract]{causal_llm_extract()}}.}
}
\value{
Invisibly, the list of records written.
}
\description{
Runs \code{\link[=causal_llm_extract]{causal_llm_extract()}} (real Ollama / OpenAI / Anthropic) or
the deterministic \code{\link[=llm_benchmark_simulate]{llm_benchmark_simulate()}} fallback over every
abstract in the corpus, and writes a \strong{draft JSONL} in the same
schema as \code{cerrado_gold_standard_v1.jsonl} but with \code{claims}
marked \code{status = "draft"} so the Shiny reviewer can distinguish
machine-generated entries from human-added ones.
}
